---
title: Robinson 產品開發階段紀錄
spec: docs/specs/robinson/SPEC.md
updated: 2026-08-10
---

# Robinson 產品開發階段紀錄

> 本文件追蹤「產品階段」層級的進度（Phase 完成度、里程碑、待決事項），細部任務進度請看 [SPEC.md](./SPEC.md) 的 checkbox。每完成一個 Phase 的所有 Step，回來更新本文件的階段狀態與里程碑。

## 專案緣起（Claude Code 協作開始前）

- **2026-07-28**：Robin 自行完成所有外部服務的註冊與 API 金鑰申請、Telegram Bot 基礎設定；並與 Gemini 進行腦力激盪與方案收斂 —— 梳理生活痛點、評估技術可行性、把發散的想法轉化為具體的 PRD（Product Requirement Document）雛形
- **2026-07-29**：正式開始與 Claude Code 協作，產出標準規格書（`docs/specs/robinson/SPEC.md`）與 Codebase 規範等文件

## 目前階段

**Phase 1（MVP）全部 Step 完成；Phase 2（記帳＋體態管理＋重要通知＋系統韌性與自主診斷治理）全部 7 個 Step 完成；Phase 3（個人技能成長＋好友模式）全部 5 個 Step（3.1～3.5）皆已完成，Phase 3 至此全數完成，下一步進入 Phase 4**

## 目標時程（2026-07-30 更新：改為三週制，因新增多模態影像/語音處理架構；2026-08-04 更新：Phase 5「Notion 後台」取消，Mobile App 併入 Phase 4，見 SPEC.md ADR-14）

- **Phase 0～4：2026-07-29 ～ 2026-08-18（三週）**，Mobile App（原 Notion 後台）已併入 Phase 4，不再是獨立的最終階段

原本兩週＋1 天緩衝（7/29～8/12）的規劃，因本次新增大量架構性內容而順延至三週：① 四把 Gemini Key 依用途分流＋語音改用 Groq Whisper（ADR-12）② 影像/語音「先上雲端、後壓縮、再辨識」的完整處理流程與命名規則（ADR-13）③ `/function` 全面改版為「總覽＋按需深入＋情境範例」＋所有對話回覆需符合人格化語氣（FR-56～FR-56d）④ 影像辨識的個資警語、不確定需詢問、飲食誤差聲明（FR-17～FR-17c）。這些改動主要集中在 Phase 1 Step 1.3（Gemini 對話核心）附近，新增 Step 1.3a／1.3b，工作量顯著增加，Robin 已同意延長至三週（若進度超前則不用真的用滿三週）。

### 建議每日分配（僅供參考，Robin 可依實際進度調整）

| 日期 | 建議內容 |
| --- | --- |
| 7/28（已完成） | 專案緣起：服務註冊/API 申請、Telegram Bot 基礎設定、Gemini 腦力激盪收斂 PRD 雛形（非 Claude Code 協作範圍） |
| 7/29（已完成） | Phase 0：`submodules/` 骨架、規格書初版確認 |
| 7/30（已完成） | Phase 0 全部完成（金鑰串接、DB 建表、`/healthz` 上線、cron-job.org）；Phase 1 Step 1.1（通關密碼驗證＋Owner 設定對話流＋歡迎訊息＋`/rule`／`/function`）完成；新增四把 Gemini Key／Groq Key 與影像/語音處理架構（ADR-12、ADR-13） |
| 7/31 | Step 1.2（功能開關系統） |
| 8/1～8/3 | Step 1.3（Gemini 對話核心＋四把 Key 分流＋人格化語氣）、Step 1.3a（`/function` 改版）、Step 1.3b（影像辨識基礎流程：GDrive 上傳＋Pillow 壓縮＋雙 Key 隨機辨識），內容較多獨立抓 3 天 |
| 8/4 | Step 1.4（語音轉文字改用 Groq Whisper＋GDrive 備份＋10 分鐘上限＋15 分鐘文字修正限制） |
| 8/5 | Step 1.5（個資偵測與刪除機制） |
| 8/6 | Step 1.6（基礎錯誤處理層）＋ Step 1.7（待辦事項模組） |
| 8/7 | Step 1.8（心情小記）＋ Step 1.9（客訴收集），若進度落後可將 Step 1.9 挪到 Phase 2 之後 |
| 8/8～8/9 | Phase 2：記帳、體態管理（含飲食誤差聲明 FR-17c）、重要通知 |
| 8/10～8/11 | Phase 2：Step 2.4（**2026-08-05 範疇簡化，見 SPEC.md ADR-15**：錯誤 log 雲端連結，難度大幅下降，複用既有 `GDriveClient`）～2.6（重試機制、分級降級）|
| 8/12～8/14 | Phase 3：技能成長（TOEIC 雙軌 Pipeline，語音改用 Groq Whisper）＋ YouTube 技術情報模組 ＋ 好友模式 |
| 8/15～8/16 | Phase 4：104 求職爬蟲＋整合測試＋ Mobile App（Step 4.4／4.5 Placeholder，詳細規劃留待開工時展開） |
| 8/17～8/18 | 全 Phase 整合測試／緩衝日 |

## 階段總覽

| Phase | 內容 | 狀態 | 目標日期 | 備註 |
| --- | --- | --- | --- | --- |
| Phase 0 | 專案基礎建設（repo 結構、金鑰串接、Render/Neon/cron-job、DB 初始化） | 🟢 已完成 | 7/29～7/30 | 全部 Step 完成：`submodules/`、`src/schema/`、`src/migrations/`（ADR-11）骨架就緒；`/healthz` 已部署上線並掛上 cron-job.org；第一批 5 張表（`users`／`invite_codes`／`knowledge_base`／`conversation_logs`／`feature_toggles`）已核准並套用成功 |
| Phase 1（MVP） | 核心平台（通關密碼對話式設定、歡迎訊息、`/rule`／`/function`／`/complaint` 內建指令、功能開關、Gemini 對話+知識庫、影像辨識、語音、個資遮蔽、基礎錯誤處理）＋待辦事項＋心情小記＋客訴收集 | 🟢 已完成 | 7/31～8/2 | Step 1.1～1.9 全數完成（實際 8/2 就全數完成，早於原訂 8/7 目標）；FR-56 全面改版為總覽＋按需深入＋情境範例（FR-56a～FR-56d） |
| Phase 2 | 記帳＋體態管理＋重要通知＋錯誤 log 雲端連結＋重試機制＋分級降級＋Google Calendar 整合 | 🟢 已完成 | 8/8～8/11 | 全部 7 個 Step 皆已完成，實際 8/7 就全數完成，早於原訂 8/11 目標。Step 2.1（記帳，FR-41～FR-44）、Step 2.2（體態管理，FR-45～FR-48）、Step 2.3（重要通知，FR-53）皆已於 8/4 完成；Step 2.4 已於 8/5 完成（範疇依 SPEC.md ADR-15 簡化為錯誤 log 雲端連結）；Step 2.7（Google Calendar 整合，FR-66）已於 8/5 完成；Step 2.5（外部 API 重試機制，FR-19i）已於 8/7 完成，新增共用 `submodules/retry` 工具；Step 2.6（例外分級降級與決策執行狀態閉環回饋，FR-19f～FR-19h）已於 8/7 完成，`webhook.py` 新增一般感冒級／重大疾病級分類 |
| Phase 3 | 個人技能成長（每日技術摘要＋TOEIC 雙軌題庫 Pipeline＋YouTube 技術情報模組，僅 Robin）＋好友模式 | 🟢 已完成 | 8/12～8/14 | 全部 5 個 Step（3.1～3.5）皆已於 8/8 完成，早於原訂時程。Step 3.1（每日重點技術分享，FR-22／FR-23）、Step 3.2（TOEIC 雙軌題庫 Pipeline，FR-25a～FR-25f）於 8/7 完成；Step 3.3（作答紀錄、成效追蹤與正式成績，FR-24、FR-26～FR-30）、Step 3.4（YouTube 技術情報模組，FR-57～FR-59，見 ADR-21）、Step 3.5（好友模式，FR-51、FR-52，見 ADR-22）皆於 8/8 完成；TOEIC 語音處理改用 Groq Whisper（ADR-12），整包 MP3 自動切割邏輯屬啟發式判斷、待真實素材驗證（見 SPEC.md ADR-18） |
| Phase 4 | 求職模組（104 爬蟲＋評分）＋ Mobile App（BI Dashboard，React Native + Expo） | 🟡 進行中 | 8/15～8/16 | 原獨立拆出的 Phase 5（Notion 後台）已取消，Mobile App 併入本 Phase（Step 4.4／4.5，皆為 Placeholder），見 SPEC.md ADR-14，詳細規劃留待開工時展開；**2026-08-09**：**Step 4.1（104 職缺爬蟲＋公司背景 Email 協作＋履歷收集，FR-33～FR-36，見 ADR-24、ADR-26）全數實作完成並經 Robin 真實 API 驗證**；**同日 Step 4.2（Gemini 契合度評分＋技能缺口分析＋雙重排名 Excel 交付，FR-37、FR-38，見 ADR-26）全數實作完成**；**同日 Step 4.3（應徵成效追蹤＋外部管道職缺，FR-39、FR-40，見 ADR-27）全數實作完成**（`job_applications` 應徵歷程表、`job_postings`／`job_companies` 新增 `source` 欄位讓外部職缺與 104 職缺共用同一套評分/排名邏輯，詳見下方里程碑）。**Phase 4 求職模組主線（Step 4.1～4.3）至此全數完成**，僅剩 Step 4.4／4.5（Mobile App）為 Placeholder 尚未開工；**同日 Mobile App 規格盤點與定案（見 ADR-28）**：App 範疇由唯讀 BI Dashboard 擴大為「唯讀分析＋可編輯設定管理」、登入機制改帳密＋Token，實作仍待開工 |

狀態圖例：⚪ 未開始　🟡 進行中／規劃中　🟢 已完成　🔴 阻塞

## 里程碑紀錄

| 日期 | 里程碑 |
| --- | --- |
| 2026-07-28 | 專案緣起：Robin 完成服務註冊/API 金鑰申請、Telegram Bot 基礎設定，與 Gemini 腦力激盪收斂 PRD 雛形 |
| 2026-07-29 | 完成需求彙整，建立 `docs/specs/robinson/SPEC.md`（產品規格書） |
| 2026-07-29 | 建立本開發階段紀錄文件 |
| 2026-07-29 | 調整 FR-15（語音修正限制改為 15 分鐘窗口） |
| 2026-07-29 | 完成 `submodules/` 共用子模組骨架（`neon_postgres`、`telegram_client`、`gemini_client`），新建 [docs/specs/submodules-core/SPEC.md](../submodules-core/SPEC.md) |
| 2026-07-29 | `submodules/` 依 Robin 指定樣板重構：更名為 `llm`/`cloudsql`/`telegram`，統一四檔案結構 |
| 2026-07-29 | 重寫 FR-19：錯誤處理擴充為 5 步驟自主診斷流程（新增 ADR-7），Phase 1 範圍縮小、AI 診斷延後至 Phase 2 |
| 2026-07-30 | **7 項待確認事項全數回覆，Phase 1 正式解除阻塞**：MVP 分期（同意）、通關密碼設定改為 Owner 對話流（新增 FR-6a～FR-6c、ADR-8）、TOEIC 雙軌 Pipeline（新增 FR-25a～FR-25f）、104 爬蟲技術細節定案（新增 FR-34a～FR-34c，頻率改每週一次）、Notion 拆為獨立 Phase 5、個資遮蔽規則細化（新增 FR-13a～FR-13d）、FR-19e 執行機制定案為 GitHub PR 治理模式 |
| 2026-07-30 | 新增 FR-19f～FR-19i（例外分級降級「一般感冒級/重大疾病級」、決策執行狀態閉環回饋、外部 API 重試機制）與 NFR-9、NFR-10；FR-19d 補充「程式碼異動紀錄」要求 |
| 2026-07-30 | 新增 `docs/profile/Robinson.png`（Robinson 大頭照，**永久禁止刪除**），已記錄於 SPEC.md「重要資產」章節 |
| 2026-07-30 | 新增 `GITHUB_TOKEN`／`GITHUB_REPO` 至 `.env.example`，同步更新 NFR-5 |
| 2026-07-30 | 新增 FR-6d（通關密碼驗證成功歡迎訊息）、FR-55（`/rule` 路由）、FR-56（`/function` 路由）；新增「附錄 A：規範文本」存放固定歡迎訊息全文；`/function` 的實際文字模板因尚未有產品原型暫緩，記錄於附錄 B 待補清單 |
| 2026-07-30 | 補上「專案緣起」段落（見上方，2026-07-28 的 Robin 個人準備工作）；目標時程由一週改為兩週，Phase 5（Notion）順延至 8/11 之後 |
| 2026-07-30 | 新增「YouTube 技術情報模組」（FR-57～FR-59、ADR-9）：每週四自動推播 Top 3 技術影片，三層輕量篩選（格式過濾/相關度評分/歷史去重），配額 100 Units/次、每日上限 1,000 Units；新增 NFR-11 排程 ETL 去重通則，回頭補上 FR-34d（104 職缺 ETL 去重）；新增 `YOUTUBE_API_KEY` 金鑰 |
| 2026-07-30 | Phase 3 因新增 YouTube 模組由 2 天延長為 3 天（8/7～8/9），Phase 4／緩衝日順延 1 天，Phase 5（Notion）目標日期改為 8/12 之後 |
| 2026-07-30 | 概要新增「使用性質聲明」（個人非商業用途），新增 NFR-13 |
| 2026-07-30 | 新增 ADR-10（資料庫 Schema 建立採先審核後執行流程）與 NFR-12；建立 `src/schema/db_schema.md`、`src/schema/api_schema.md` 骨架；Phase 0 新增 Step 0.1b（已完成），Step 0.5 改為依 ADR-10 流程逐一審核建表 |
| 2026-07-30 | 新增客訴收集功能 FR-60～FR-63：`/complaint` 路由、客訴內容記錄、Gemini 分析私訊 Robin、Robin 人工決策；Phase 1 新增 Step 1.9 |
| 2026-07-30 | 附錄 A 開頭語句改為「📋 以下是羅賓森的使用須知：」（原「🎉 通關密碼驗證成功！」在 `/rule` 場景語意不通順），並補上「我要客訴你」觸發提示 |
| 2026-07-30 | Phase 0 啟動連線驗證時，測試腳本意外將 `TELEGRAM_BOT_TOKEN`、`YOUTUBE_API_KEY` 明文印出於對話紀錄中（原因：`requests` 例外訊息包含完整請求 URL，兩者金鑰恰好嵌在 URL 裡）；Robin 已於當天完成兩把金鑰重新產生，逐項覆核確認無其他金鑰外洩（`ROBIN_TELEGRAM_TOKEN` 部分曝光但經 Robin 確認為 Telegram 使用者 ID、非機密憑證，且僅曝光數字 ID 本身）；已修正 `submodules/telegram/README.md` 對 `ROBIN_TELEGRAM_TOKEN` 用途的錯誤描述 |
| 2026-07-30 | 發現 Cowork sandbox 對外部服務有網路白名單限制：連不到 Neon／Telegram／`api.github.com`／Google 與 YouTube API／Notion API；但 `github.com`（git 協定）可連線，並實測 `git push`（搭配 `GITHUB_TOKEN` + credential helper）可成功。新增 ADR-11：ADR-10 的執行機制改為「提出 SQL → Robin 同意 → Claude 建立 `src/migrations/` 檔案並 commit+push → Render 偵測 main 分支自動部署 → `main.py` 開機自動套用」；Robin 確認 Render 已開啟 push-to-main 自動部署，此方案可行；Phase 0 新增 Step 0.5a |
| 2026-07-30 | Step 0.5a 完成：建立 `src/migrations/`（runner.py + README）、`CloudSQLClient` 新增 `execute()`、`main.py` 整合開機自動套用；完成首次 commit + push 到 GitHub main（`5f60602..776802f`），觸發 Render 自動部署，待 Robin 於 Render 確認 `/healthz` 可正常連線後即完成 Step 0.3 |
| 2026-07-30 | Step 0.3 完成：Robin 於 Render Dashboard 確認部署成功（`Your service is live`），正式網址 `https://life-assistant-bot-yhkm.onrender.com`；下一步由 Robin 把 `/healthz` 端點加到 cron-job.org（Step 0.4） |
| 2026-07-30 | Step 0.4 完成：Robin 已於 cron-job.org 設定每 10 分鐘呼叫 `/healthz`，確認 API 正常。**Phase 0 僅剩 Step 0.5（Neon 資料庫初始化）**，其餘全數完成 |
| 2026-07-30 | Step 0.5 第一批 5 張表核准並 push：`users`／`invite_codes`／`knowledge_base`／`conversation_logs`／`feature_toggles`（`776802f..e440b7c`），已記錄到 `src/schema/db_schema.md`；ADR-10 新增第 5 點：所有建表 SQL 必須用 `COMMENT ON TABLE`／`COMMENT ON COLUMN` 附中文說明 |
| 2026-07-30 | Robin 於 Render 部署 log 確認 5 筆 migration 全數套用成功（`0001`～`0005` 皆有「套用／完成」紀錄）。**Phase 0（專案基礎建設）全部 Step 完成**：Step 0.1～0.1b、0.2～0.5 皆已完成，可正式進入 Phase 1（MVP） |
| 2026-07-30 | **Phase 1 Step 1.1 完成**：通關密碼驗證、Owner `/set_invite_codes` 對話式設定流程、FR-6d 歡迎訊息、`/rule`／`/function` 內建指令，展開為獨立 [docs/specs/platform-auth/SPEC.md](../platform-auth/SPEC.md)；新增 ADR-1（webhook 改用原生 JSON 解析、移除 `python-telegram-bot`）、ADR-2（對話狀態存記憶體不落地資料庫）；新增 `src/bot/`（`state.py`／`auth.py`／`templates.py`／`commands.py`／`router.py`／`webhook.py`），49 個測試全過、覆蓋率 100%；新增 `requirements-dev.txt`／`pytest.ini` |
| 2026-07-30 | 測試通過後，Robin 提出多模態與人格化語氣的大改版需求：新增四把 Gemini Key＋Groq `VOICE_API_KEY`（ADR-12，語音改用 Groq Whisper，取代 FR-25b 原「一律用 Gemini」決策）；新增 ADR-13（影像/語音先上雲端、`Pillow` 壓縮、統一命名、URL 入庫）；FR-17 開放一般圖片辨識並新增個資警語/不確定需確認/飲食誤差聲明（FR-17a～FR-17c）；FR-56 全面改版為總覽＋按需深入＋情境範例（FR-56a～FR-56d，記帳範例已由 Robin 提供）；`src/bot/templates.py` 附錄 A 文字同步更新；新增 `src/migrations/0006_seed_persona_and_family_knowledge.sql` 寫入 Robinson 人格背景與家人背景資料；時程由兩週延長為三週（8/12 → 8/18），Phase 1 目標日期順延至 8/7 |
| 2026-07-30 | Robin 確認 Render 部署 log 顯示 `0006` migration 套用成功，Robinson 人格背景與 Robin 家人背景已寫入 Neon `knowledge_base` 表，供未來人格化回覆（FR-56c）讀取 |
| 2026-07-30 | Robin 確認 Step 1.2 功能開關權限模型（FR-2a：使用者可自管、Owner 可代管），展開為獨立 [docs/specs/feature-toggles/SPEC.md](../feature-toggles/SPEC.md)（ADR-1：對話狀態 dict 新增 `flow` 欄位）；**Phase 1 Step 1.2 完成**：新增 `src/bot/toggles.py`、`/my_toggles`（自管）、`/set_toggle`（Owner 代管）；家人第一次綁定成功時自動建立 8 筆預設開啟的 `feature_toggles`；`src/bot/` 全部 78 個測試全過、覆蓋率 100% |
| 2026-07-31 | Robin 確認查無答案時採「單次 API 呼叫＋Google Search grounding」（見 chat-core SPEC.md ADR-1），展開為獨立 [docs/specs/chat-core/SPEC.md](../chat-core/SPEC.md)（ADR-2：`pending_kb_save` 狀態流程）；**Phase 1 Step 1.3 完成**：一般聊天訊息正式交給 Gemini 處理（取代 `_PLACEHOLDER_REPLY`），新增 `src/bot/knowledge.py`（知識庫查詢/寫入、資安隔離）、`src/bot/chat.py`（對話核心、存檔確認流程），`submodules/llm/client.py` 新增 `generate_with_search()` 並補上單元測試；全專案 104 個測試全過、覆蓋率 100% |
| 2026-07-31 | Robin 指出短記憶會忘記久遠對話，確認記憶架構改為「長記憶＋短記憶＋知識庫＋上網查資料」四部分並核准 `conversation_summaries` 建表 SQL；新增 ADR-3（滾動式摘要）：新增 `src/bot/memory.py`（backlog ≥10 則觸發、呼叫 `GEMINI_API_TEXT_KEY`），`chat.py`／`router.py`／`webhook.py` 整合第二把 Gemini Key；全專案 117 個測試全過、覆蓋率 100% |
| 2026-07-31 | Robin 提供待辦事項／求職／體態管理／心情小記情境範例（新增 FR-56e～FR-56h），補充 FR-31a（待辦逾期標記）與 FR-46（身高體重合理範圍檢查）業務規則 |
| 2026-07-31 | **Phase 1 Step 1.3a 完成**：`/function` 重新實作為「總覽＋按需深入＋情境範例」，展開為 chat-core SPEC.md FR-9／ADR-4（總覽獨立小型 LLM 呼叫，細節追問併入既有聊天核心，Robin 確認）；`commands.handle_function()` 改為 LLM 人格化總覽，`chat.py` prompt 固定附上功能手冊（含 FR-56d～FR-56h 情境範例）供按需回答；全專案 126 個測試全過、覆蓋率 100% |
| 2026-07-31 | Robin 實測撞到 Gemini 429，發現 `webhook.py` 未攔截例外會讓 Telegram 重試風暴加速燒額度，新增 platform-auth SPEC.md FR-7（安全網：`try/except` + 固定安全用語 + 仍回 200） |
| 2026-07-31 | Robin 要求「該做的防呆要做好」，再補兩層防護：platform-auth SPEC.md FR-7a（`update_id` 去重，防止「沒出錯但被誤判逾時重送」重複打 Gemini）與 submodules-core SPEC.md FR-7／ADR-5（`LLMClient` 本地端節流保護，同一 `api_key` 最近 60 秒超過 8 次呼叫直接擋下不送出請求）；全專案 137 個測試全過、覆蓋率 100% |
| 2026-07-31 | Robin 確認 429 為真實額度超限（安全網運作正常），並確認 Step 1.3b 設計：`media_uploads` 表統一記錄圖片/語音 Drive 網址、壓縮版圖片僅記憶體內處理不落地存 Drive（修正 ADR-13）；建立 `media_uploads` migration 並 push |
| 2026-07-31 | **Phase 1 Step 1.3b 完成**：影像辨識基礎流程，FR-17／FR-17a～FR-17c 全數完成；新增 `submodules/gdrive/`（僅上傳、Service Account 認證）、`TelegramClient.get_file_bytes()`、`src/bot/image.py`（`Pillow` 壓縮＋隨機挑選影像 Key＋`[NEED_CONFIRM]` 標記反問使用者）；`router.py`／`webhook.py` 完成圖片訊息與不支援格式的整合；全專案 179 個測試全過、覆蓋率 100% |
| 2026-07-31 | Robin 換用新產生的 `GEMINI_API_BOT_KEY` 後，`generate_with_search()` 固定使用的 `gemini-2.5-flash` 回傳 404；排查後確認 Gemini 2.5 世代已對新專案關閉存取（用 curl 直測 `generateContent` 驗證，跟掛不掛 Google Search 工具無關），非額度問題；Robin 指示「移除所有會用到上網查詢的部分，查無答案就誠實回不知道，並建議使用者自行查詢後提供答案存檔」；新增 chat-core SPEC.md ADR-5（supersede ADR-1／ADR-2）、submodules-core SPEC.md ADR-8（supersede ADR-7）：`generate_with_search()` 全數移除，`pending_kb_save` 流程更名為 `pending_user_knowledge` 且不再需要 yes/no 確認；全專案 174 個測試全過、覆蓋率 100% |
| 2026-08-01 | Robin 陸續回報並確認多項 chat-core 修正與新功能（日期幻覺、代名詞指涉、打字誤植先反問確認、`/clean-all-dialog`／`/clean-target-dialog`、主動新增知識等，詳見 [chat-core SPEC.md](../chat-core/SPEC.md) 變更記錄 ADR-6～ADR-8），全專案累計到 219 個測試、覆蓋率 100% |
| 2026-08-01 | **Phase 1 Step 1.4 完成**：語音轉文字流程，FR-14／FR-15 全數完成；新增 `submodules/voice/`（`VoiceClient`，`requests` 直打 Groq Whisper OpenAI 相容 REST API，見 submodules-core SPEC.md ADR-9）、`src/bot/voice.py`（10 分鐘上限／15 分鐘修正窗口檢查、上傳 Drive＋轉文字）；架構決策：轉出來的文字直接呼叫既有 `router.handle_message()` 走完整文字流程，不重複指令/對話流程分派邏輯；`src/bot/media.py` 從 `image.py` 抽出共用的 `save_media_upload()`；全專案 252 個測試全過、`src/bot/`／`submodules/llm`／`submodules/voice` 覆蓋率 100% |
| 2026-08-01 | **Step 1.4 追加修正**：Robin 回報「除了照片和音檔外的檔案格式才無效」，發現 Step 1.4 完成當下只處理了 `message.voice`（錄音鍵語音訊息），漏了 `message.audio`（使用者上傳的音檔，如 MP3）——FR-17 原文早就承諾「圖片與音檔兩種檔案類型」都支援，這是範圍沒抓對，不是新功能；修正 `webhook._extract_voice()` 同時偵測 `voice`／`audio` 並透傳 `mime_type`，新增 `voice._infer_extension()` 依實際格式決定 Drive 副檔名與 Groq 轉錄格式，避免把上傳的 MP3/M4A/WAV 誤標成 `.ogg`；全專案 262 個測試全過、`src/bot/`／`submodules/llm`／`submodules/voice` 覆蓋率 100% |
| 2026-08-02 | **新增 FR-16a（語音最終執行確認）**：Robin 問語音轉文字後是直接執行還是會先確認，得知是直接執行後，提出風險情境「語音說 A 決策被聽錯成 B 決策，且已刪除的紀錄無法回頭補上」；選定「復誦＋最終執行前一定要打字答一次」方向，`/clean-all-dialog`／`/clean-target-dialog`／主動記知識三個高風險 flow 新增 `pending_*_final_confirm` 狀態，判定 `CONFIRM` 後不再馬上執行，改為要求逐字打字輸入「確認執行」；語音輸入這一步一律拒絕且不清除狀態，詳見 [chat-core SPEC.md](../chat-core/SPEC.md) ADR-9；全專案 274 個測試全過、覆蓋率 100% |
| 2026-08-02 | **FR-16a 追加優化**：Robin 追問卡在最終確認狀態時收到新語音會如何處理，發現初版是先下載/轉錄才拒絕，浪費 Drive/Groq 額度，確認補上；`handle_voice_message()` 改為在下載/轉錄之前就短路拒絕，完全不消耗額度；全專案 275 個測試全過、覆蓋率 100% |
| 2026-08-02 | **補上 FR-14 規則 1（語音超時全面鎖定）**：Robin 指出印象中 15 分鐘鎖定應該是「單次錄音超過 10 分鐘才觸發」，核對後發現這其實跟 FR-15「修正情境鎖定」是兩條獨立規則，目前只做了後者，前者完全沒實作；確認兩條都要做，新增 `voice.mark_duration_violation()`／`is_locked_out_from_duration_violation()`，`webhook.py` 新增長期持有的 `_voice_lockout_store`；全專案 284 個測試全過、覆蓋率 100% |
| 2026-08-02 | **FR-15 修正窗口主動提醒**：Robin 追問語音功能被限制/恢復時會不會提醒使用者；盤點後發現 FR-14 規則 1 的拒絕回覆已有主動提示，但 FR-15 修正窗口開始當下、鎖定到期時都沒有主動通知（機器人被動回應訊息，沒有排程/推播機制）；Robin 選擇先聚焦較簡單的一項，新增 `router._VOICE_TRANSCRIBED_REMINDER`，語音成功轉出文字後於回覆末尾附註 15 分鐘修正窗口提醒；鎖定到期主動通知維持現狀；全專案 284 個測試全過、覆蓋率 100% |
| 2026-08-02 | **修正 Telegram send_text 400 錯誤 + 排查 gdrive 金鑰路徑**：Robin 實測回報兩個部署後問題：① `/function` 觸發 Telegram `sendMessage` 400 Bad Request，根因是 `send_text()` 預設 `parse_mode="Markdown"`，但 LLM 生成的回覆文字無法保證符合 Telegram 舊版 Markdown 語法，格式不符會整則被拒收——所有 LLM 生成回覆都有此風險，非 `/function` 獨有；Robin 選擇直接關閉 Markdown，改為預設純文字傳送 ② 語音功能因 `GDriveClient` 找不到 Service Account 金鑰檔而失敗，確認是 Robin 把金鑰放在 Render Secret Files（實際掛載路徑 `/etc/secrets/<filename>`），但 `GDRIVE_KEY_FILE_PATH` 環境變數設定的是相對路徑，兩者對不上——純屬 Render 環境變數設定問題，非程式碼錯誤，待 Robin 調整環境變數即可解決；全專案 285 個測試全過、覆蓋率 100% |
| 2026-08-02 | **Phase 1 Step 1.5 完成**：個資偵測與遮蔽機制，FR-13／FR-13a～FR-13d 全數完成，展開為獨立 [docs/specs/privacy-masking/SPEC.md](../privacy-masking/SPEC.md)；Robin 確認語意層 LLM 呼叫改用新申請的專用 Key（`GEMINI_API_PRIVACY_KEY`），不佔用既有聊天配額（吸取先前多次 429 的教訓）；新增 `src/bot/privacy.py`（Regex 硬規則＋LLM 語意雙層防線），整合進一般聊天核心與圖片說明文字，語音因統一轉文字後走既有流程天然涵蓋；`/clean-target-dialog` 的搜尋主題刻意排除遮蔽，避免使用者用個資內容當關鍵字搜尋刪除時功能失效；全專案 326 個測試全過、覆蓋率 100% |
| 2026-08-02 | **gdrive 改用 OAuth 2.0（真人帳號身分）**：Robin 實測語音上傳撞到 Google Drive API `403 storageQuotaExceeded`，查證確認 Service Account 完全沒有 Drive 儲存額度，改用 Shared Drive 需要付費 Google Workspace；經 AskUserQuestion 確認，Robin 選擇改用 OAuth 讓程式以本人身分上傳；`submodules/gdrive/client.py` 建構子改為 `refresh_token`／`client_id`／`client_secret`／`folder_id`，新增一次性本機互動授權腳本 `get_refresh_token.py`；`webhook.py` 兩處建構呼叫與環境變數同步更新；新增 submodules-core SPEC.md ADR-10、robinson SPEC.md ADR-13 補充決策；全專案 329 個測試全過、覆蓋率 100%；**待 Robin 執行的手動步驟**：Google Cloud Console 建立 OAuth 用戶端 ID、發布狀態設為正式版、本機跑 `get_refresh_token.py` 取得 refresh token、設定 Render 環境變數 |
| 2026-08-02 | **Phase 1 Step 1.6 完成**：基礎錯誤處理層，FR-19a／FR-20／FR-21 完成（FR-19b～FR-19i 的 AI 自主診斷／分級降級／重試機制仍留待 Phase 2 Step 2.4～2.6）。經 AskUserQuestion 確認兩個範圍決策：① FR-20 新增 Owner 專屬指令 `/recovered`，Robin 判斷修好後手動觸發，廣播固定文案給所有已綁定家人（排除 Robin 自己）② FR-21 Gemini 免費額度監控 Phase 1 先跳過（官方無查詢即時用量的 API），只做 Neon 容量監控（新增 `src/bot/monitoring.py` 的 `NeonCapacityMonitor`，借用 `/healthz` 既有的 10 分鐘 cron 頻率，達 80% 私訊 Robin、回落後重置避免重複告警）；FR-19a：`webhook.py` 新增 `_notify_robin_of_error()`／`_summarize_user_input()`，例外發生時除了記錄 Traceback＋情境到 log，額外私訊 Robin 完整原始內容；`submodules/cloudsql/client.py` 新增 `execute_query()` 供監控查詢使用；全專案 352 個測試全過、覆蓋率 100% |
| 2026-08-02 | **Phase 1 Step 1.7 完成**：待辦事項模組，FR-31／FR-31a／FR-32 完成。經 AskUserQuestion 確認兩個範圍決策：① 待辦意圖偵測比照 FR-11「主動新增知識」的 LLM 標記模式（新增 `chat.py` 的 `_REQUEST_TODO_MARKER`），符合「自然語言描述」的精神，不用新增固定指令 ② FR-31 跨模組歧義判斷 Phase 1 暫不實作（體態管理／心情小記都還沒做，沒有其他模組可以比較）。新增 `todos` migration（Robin 依 ADR-10 核准）；`src/bot/todo.py` 新增/查詢/標記/逾期/推播判斷純邏輯；新增流程走三輪反問（要不要記錄→什麼時候→要不要提前 30 分鐘提醒），時間解析不清楚時停留原地繼續問；查詢清單「我的待辦事項」／`/my_todos` 後可選編號標記完成/取消；每日 08:00 固定推播與前 30 分鐘提醒比照 Step 1.6 借用 `/healthz` 頻率（`main.py` 新增 `_check_todo_pushes()`），但去重狀態存在 `todos` 資料列本身而非記憶體，跨 Render 重啟仍正確；全專案 391 個測試全過、覆蓋率 100% |
| 2026-08-02 | **Phase 1 Step 1.8 完成**：心情小記模組，FR-49／FR-50 完成。經 AskUserQuestion 確認兩個範圍決策：① 日記內容與 FR-50 個人成就回答都套用 FR-13 個資遮蔽，新增第四個遮蔽入口（跟一般聊天／圖片說明文字／語音轉文字一致）② 新建 `mood_journals` 表（Robin 依 ADR-10 核准）。流程分三輪：「我想做心情筆記」／`/mood_journal` 先問心情分類（固定 6 選一，接受編號或名稱）→ 問日記內容並寫入 → 主動追問 FR-50 個人成就三選一提示（可跳過）；全程不需要 LLM，比 Step 1.7 待辦事項簡單（心情分類固定選項、個人成就有填就存沒填就跳過，純字串比對即可）；新增 `src/bot/mood.py`、`commands.py` 三個新 flow、`router.py` 整合；全專案 409 個測試全過、覆蓋率 100% |
| 2026-08-02 | **Phase 1 Step 1.9 完成，Phase 1（MVP）全數 Step 完成**：客訴收集模組，FR-60～FR-63 完成。經 AskUserQuestion 確認兩個範圍決策：① 客訴內容仍套用 FR-13 個資遮蔽，FR-62 的隱私例外只針對「Robin 平常看不到家人個別對話」這條規則，跟個資保護是不同層面的考量 ② 新建 `complaints` 表（Robin 依 ADR-10 核准），Gemini 分析結果只私訊 Robin、不落地存表。流程分兩輪：「我要客訴你」／`/complaint` 固定提問（不經 LLM）→ 下一則訊息寫入客訴記錄後立即呼叫 Gemini 分析、私訊給 Robin（不回傳給提出客訴的使用者本人）；分析／私訊失敗不影響客訴已成功記錄；新增 `src/bot/complaint.py`、`commands.py` 兩個新 flow、`router.py` 整合（並補上 `_dispatch_active_flow()` 缺少的 `telegram_client` 透傳）；全專案 417 個測試全過、覆蓋率 100%。**Phase 1（MVP）全部 9 個 Step 完成，實際完成時間 8/2，早於原訂 8/7 目標**，下一步進入 Phase 2 |
| 2026-08-02 | **Bug 修正：「完全不理我」空回覆防呆**：Robin 實測回報打「我要載妹妹到水里」完全沒收到任何回覆（連安全用語都沒有），比對截圖確認上一則訊息已先觸發 `pending_user_knowledge` 狀態；診斷根因：`webhook.py` 原本只在「拋出例外」時才確保回覆非空，沒拋例外但這次處理結果剛好是空字串（例如 Gemini 生成回傳空內容）時，`if reply:` 判斷為 False，完全不會呼叫 Telegram 送訊息，等於違反 FR-19h「嚴禁靜默」的精神；修正新增獨立空字串防呆與專屬 fallback 文案 `_EMPTY_REPLY_FALLBACK`（跟例外安全語區分措辭），並記警告 log；全專案 419 個測試全過、覆蓋率 100% |
| 2026-08-02 | **Bug 追加修正（Robin 指出上一輪不是真正主因）＋兩個待辦事項問題**：Robin 完整重現流程後指出，真正根因是 chat-core ADR-6 三選一分流 prompt 把「無關新內容」選項寫成「問一個新的『問題』」，使用者講陳述句（不是問句）時容易被誤套進「拒絕」選項；修正為「除了以上情況以外的任何內容」的 catch-all 措辭。同時 Robin 額外發現：① 待辦時間解析對「5:30」這種沒講上午/下午、沒講哪一天的模糊時間會自己猜測，不應該擅自猜；改為日期與時段都要有明確線索才算清楚，否則反問 ② 建立待辦後固定講「當天早上 8 點會提醒你」，但若已經過了今天 8 點這句話根本不可能發生；改為依現在時間與 due 日期動態調整文案；全專案 421 個測試全過、覆蓋率 100% |
| 2026-08-02 | **新增 FR-31b：待辦事項支援時間區間**：Robin 問「待辦事項是不是只能存單一時間點，不能存像『8/2 08:00～8/5 17:00』這種區間」，確認需要後開發。經 AskUserQuestion 確認：① `todos` 新增可選 `start_at` 欄位（Robin 依 ADR-10 核准 `0016_add_start_at_to_todos.sql`），既有單一時間點待辦不受影響 ② 前 30 分鐘提醒對區間待辦以開始時間為基準 ③ 每日 8 點摘要對區間待辦在開始日、結束日各出現一次，去重邏輯從「曾經推播過」改為「今天是否已推播過」；時間解析 prompt 新增選填的 `START_AT` 欄位偵測區間描述，確認訊息與「我的待辦事項」清單都會依是否為區間顯示不同格式；全專案 436 個測試全過、覆蓋率 100% |
| 2026-08-02 | **FR-49 補記/更新/刪除擴充**：Robin 提出「記帳、心情小記、體重、飲食、運動習慣都要有補記、更新、刪除、新增的功能」，經 AskUserQuestion 確認範圍：心情小記優先實作，記帳／體態管理（Phase 2 才開始的模組）從一開始就內建 CRUD、不需要另外補。經 AskUserQuestion 確認：① `mood_journals` 新增可選 `entry_date` 欄位（Robin 依 ADR-10 核准 `0017_add_entry_date_to_mood_journals.sql`），設計比照 `todos.start_at`，一律由 app 端算好台灣時區日期後寫入 ② 刪除確認採簡單一輪 LLM CONFIRM/CANCEL（跟待辦事項完成/取消同等級），不套用 FR-16a 逐字打字最終確認（那三個高風險 flow 是「一旦誤刪就大量、跨紀錄不可逆遺失資料」，心情小記單筆刪除錯了還能重新補記）。新增「我要補記心情」／`/backfill_mood`（先問哪一天，LLM 解析日期，只接受今天或過去）與「我的心情紀錄」／`/my_mood_journals`（列出最近 10 筆，選編號後可更新或刪除）；`mood.py` 新增 `list_mood_journals()`／`format_mood_journal_list()`／`update_mood_journal()`／`delete_mood_journal()`；既有分類/內容兩步驟改為同時支援新增與編輯模式；全專案 465 個測試全過、覆蓋率 100% |
| 2026-08-04 | **Phase 1（MVP）全部完成，進入 Phase 2；Phase 2 Step 2.1 完成：記帳模組（FR-41～FR-44）**。Robin 確認「下個階段就是 Phase 2 了」後指示「請開始開發」，經 AskUserQuestion 確認範圍與 SQL：① FR-41「理財目標」解讀為每月支出預算上限（單一數字），不需要收入-支出結餘概念，但交易紀錄仍支出/收入兩種都做，保留未來彈性 ② 交易分類固定清單，比照心情小記 ③ FR-44 視覺化這版先做文字摘要，不做圖表、不做排程推播月報，「定期」交給 FR-43 門檻預警負責 ④ `users` 新增 `monthly_budget`／`budget_alert_50_sent_month`／`budget_alert_80_sent_month` 三欄、新建 `transactions` 表（Robin 依 ADR-10 核准 `0018_add_budget_fields_to_users.sql`／`0019_create_transactions_table.sql`）⑤ FR-43 兩門檻觸發時機：50% 只在每月 15 日前檢查（早期警示）、80% 整月都檢查（嚴重警示），各自每月最多推播一次。記帳從一開始就內建完整 CRUD：新增「設定記帳預算」／`/set_budget`、「我要記帳」／`/add_transaction`（類型→分類→金額→備註四輪反問）、「我要補記帳」／`/backfill_transaction`、「我的記帳紀錄」／`/my_transactions`（可選編號更新/刪除）、「我的記帳摘要」／`/my_finance_summary`；新增 `src/bot/finance.py`（純邏輯）、`commands.py` 12 個新 flow、`router.py` 整合、`main.py` 新增 `_check_finance_alerts()` 借用 `/healthz` 頻率觸發 FR-43 推播；全專案 539 個測試全過、覆蓋率 100% |
| 2026-08-04 | **Bug 修正：個資遮蔽語意層暫時性外部錯誤導致整則訊息完全無回覆**。Robin 實測記帳擴充功能時對 Robinson 追問「這兩句話有什麼差異？」，撞到 Gemini `503 UNAVAILABLE`（模型暫時過載，非額度或程式問題），`privacy.mask_with_llm()` 呼叫未捕捉例外，導致整則訊息處理中斷、完全沒收到回覆（Owner 另外收到 FR-19a 例外通報私訊，內含完整 Traceback）。修正 `privacy.mask_text()` 呼叫語意層時新增 `try/except Exception`，任何暫時性外部錯誤都優雅降級成只回傳 Regex 層結果，理由與既有 ADR-2（Key 缺失時降級）一致，記錄為 privacy-masking SPEC.md ADR-3；完整外部 API 重試機制仍留待 FR-19i（Phase 2 Step 2.5）；全專案 579 個測試全過、覆蓋率 100% |
| 2026-08-04 | **記帳模組擴充：新增 FR-44a（月底自動推播月報）**。Robin 詢問記帳摘要會不會自動推播，確認目前只有主動查詢後，指示「記帳摘要請在每月底自動推一次月報」。經 AskUserQuestion 確認：① 推播時刻 21:00，刻意跟 FR-42a 每日提醒的 23:00 錯開 ② 只推給「這個月有生效預算或有記帳」的使用者，避免沒在用記帳功能的家人收到全部 0 元的空洞報告。新增 `users.finance_monthly_report_sent_month` 去重欄位（Robin 依 ADR-10 核准 `0022_add_finance_monthly_report_field_to_users.sql`）；新增 `finance.check_and_push_monthly_report()`（借用 `/healthz` 頻率，「是不是月底」用「明天是不是 1 號」判斷，不寫死月份天數）與共用 helper `finance._users_with_finance_activity()`；`main.py` 新增 `_check_finance_monthly_report()`；內容沿用既有 `format_monthly_summary()`；全專案 589 個測試全過、覆蓋率 100% |
| 2026-08-04 | **記帳模組擴充：新增 FR-41a（預算特殊月份覆蓋）、FR-42a（每日記帳提醒）**。Robin 實測後提出三點回饋：① 有設定預算時應每天固定時間提醒記帳，但當天已記錄就不必提醒、收入不用檢查 ② 能不能隨意改預算、是否每月自動套用、某幾個月（報稅、包紅包）固定開銷較高想單獨設定，希望設定時能問要套用全部月份還是某幾個月、調整已設定的月份時要先反問確認 ③ 也發現「調整剛剛設定的 43,000 元」跟「重新設定這個月的記帳預算」這兩句反問其實沒有差異，因為當時系統沒有「月份」維度，這正好點出①②的缺口。經 AskUserQuestion 確認設計：提醒時間 23:00；預算跟月份的關聯採「全局預設值（`users.monthly_budget`）＋特殊月份覆蓋（新表 `budget_overrides`）」設計而非逐月各自存一筆——改全局預設不動已設定的特殊月，查詢當月生效預算優先用覆蓋值、沒有才 fallback 用預設值（`finance.get_effective_monthly_budget()`）；`/set_budget` 每次都先問套用範圍，選定範圍已有舊值時一律先反問確認才能真正寫入。新增 migration `0020_create_budget_overrides_table.sql`／`0021_add_finance_reminder_field_to_users.sql`（Robin 依 ADR-10 核准）；`/set_budget` 從單輪改成最多四輪的狀態機（scope→months→confirm→amount）；新增 `finance.check_and_push_finance_reminders()`（FR-42a，比照 FR-32 每日摘要借用 `/healthz` 頻率，只在台灣時間 23 點執行）；FR-43 門檻預警、FR-44 摘要都已改用生效預算查詢邏輯，新增共用 helper `finance._users_with_effective_budget()`；月份輸入一律套用今年，尚不支援跨年設定，是本次的簡化假設；`/function` 記帳說明文案補上「以一個月為單位」提醒；全專案 577 個測試全過、覆蓋率 100% |
| 2026-08-04 | **Phase 2 Step 2.2 完成：體態管理模組（FR-45～FR-48）**。Robin 確認「下個階段就是 Phase 2.2 了」後指示開發，經 AskUserQuestion 確認四項設計：① 運動消耗卡路里改用 LLM 估算（而非 MET 公式），沿用 `GEMINI_API_BOT_KEY`，符合 FR-56g 情境3「自然口吻回覆＋估算免責聲明」示範 ② 飲食三大營養素拆算同樣沿用 `GEMINI_API_BOT_KEY`（沒有食物資料庫，只能靠 LLM 語意判斷）③ FR-45 預警情境定案為目標達成通知（體重目標即時檢查、運動目標借用 `/healthz` 頻率排程加總累積分鐘數）＋目標期限前 7 天提醒＋BMI 異常提醒（記錄體重當下就地附加）④ 身高體重/運動/飲食三個子功能的目標設定共用一張 `body_goals` 表，用 `goal_type` 區分（比照 `budget_overrides` 精神）；運動目標的 `target_value` 單位原提案用公里數，Robin 指出「不是只有跑步」，改用累積運動分鐘數（各種運動都適用的共同單位）。新建 `body_weight_logs`／`exercise_logs`／`diet_logs`／`body_goals` 四張表、`users` 新增 `height_cm`（Robin 依 ADR-10 核准 `0023`～`0027` migration）；新增 `src/bot/body.py`（BMI 計算與分級、LLM 卡路里/營養估算、目標達成判斷、FR-45 排程檢查函式）；身高體重/運動/飲食三個子功能從一開始就內建完整 CRUD（新增/補記/查詢/更新/刪除），比照記帳模組；飲食目標因太主觀（例如「飲食完美控制」）不做自動達成判斷，只能手動取消，是刻意的已知簡化；`commands.py` 新增 12 個觸發詞與對應多輪反問流程、`router.py` 完成觸發詞與 flow 分派、`main.py` 新增 `_check_body_goal_alerts()` 借用 `/healthz` 頻率；`src/bot/body.py` 達到 100% 覆蓋率，全專案 661 個測試全過 |
| 2026-08-04 | **移除 Notion 後台，改採 Mobile App（React Native + Expo）**，SPEC.md 新增 ADR-14（supersede ADR-1 的後台選型部分）。Robin 指示系統架構確立為「Telegram Bot（LUI）負責所有自然語言輸入與 CRUD 控制＋Mobile App（Rich GUI）專注唯讀 BI 圖表展示」；移除原 FR-54（Notion 相關描述），新增 FR-64（唯讀視覺化）、FR-65／FR-65a～FR-65c（多用戶登入機制：一般使用者 `user_name`／稱謂／`APP Access Token`，Robin 僅需 `user_name`／`APP Access Token`）；補充技術細節供 Phase 4 展開時參考：`/api/app/*` API 設計原則（後端算好圖表 JSON 結構回傳，App 端只負責渲染）、React Native + Expo（Expo Router）基礎路由結構、資料模型補充（`users.app_access_token`）；登入與 App 詳細功能邏輯明確標記為 Placeholder，留待 Phase 4 對應 Step 開工時再深入展開。原獨立拆出的 Phase 5（Notion）取消，相關工作併入 Phase 4（與求職模組並列，新增 Step 4.4／4.5）；本次純屬規格層級調整，未建立任何程式碼、目錄或資料表 | Claude（依 Robin 詳細指示調整規格） |
| 2026-08-04 | **Phase 2 Step 2.3 完成：重要通知模組（FR-53）**。Robin 提供完整需求，分「超級重要通知（主角不能收到，家人生日／父親節／母親節）」與「重要通知（大家都收到，元旦／除夕初一／固定 3/1 掃墓提醒／中秋／端午）」兩類，各附固定文案；經來回確認補齊已知的家人 `role`→生日對照（5 位有明確生日，弟媳/大妹婿/小妹婿/阿姨 4 位生日不詳），經 AskUserQuestion 確認缺漏生日先跳過、改為新增 Owner 自助指令補齊；經 AskUserQuestion 確認三項設計：① 農曆節日改用 `lunarcalendar` 套件即時計算西曆日期（純 Python、不需要網路，驗證過完全離線可用），不維護每年對照表 ② 固定台灣時間 08:00 推播，借用 `/healthz` 既有 10 分鐘 cron 頻率 ③ 父親節/母親節用 `users.role` 字串比對排除、家人生日用 `user_id` 排除當事人自己。新增 `users.birthday`、`important_notifications_log`（`UNIQUE(notification_key, year)` 年度去重）兩張表與已知 5 位生日的種子資料（Robin 依 ADR-10 核准三份 SQL）；新增 `src/bot/notifications.py`（純邏輯）與 Owner 專屬「設定家人生日」／`/set_family_birthday` 指令（比照既有 `/set_toggle` 流程：列出所有已綁定使用者選編號→輸入生日）；`main.py` 新增 `_check_important_notifications()` 借用 `/healthz` 頻率；`src/bot/notifications.py` 達到 100% 覆蓋率，全專案 703 個測試全過 | Claude（依 Robin 提供的需求與 AskUserQuestion 確認範圍後實作） |
| 2026-08-05 | **Step 2.4 開工前範疇簡化，SPEC.md 新增 ADR-15（supersede ADR-7）**：盤點技術現況後發現原規劃的 FR-19b「上網查詢」前提（Gemini Search grounding）已因 submodules-core SPEC.md ADR-8 被整個移除且無法復原（Robin 不考慮開通計費帳戶），FR-19e 的 GitHub API 自動開分支/PR＋LLM 自動生成程式碼異動工程量與風險都偏高；Robin 提議改為更輕量的方案：例外發生時把完整 log（Traceback／觸發功能／使用者輸入摘要）上傳 Google Drive（複用 Step 1.3b 既有的 `GDriveClient`），私訊 Robin 專屬連結，其他使用者行為完全不變（僅收到既有「生病了」安全用語，不含任何連結或技術細節，Robin 特別確認這一點）；FR-19c／FR-19d／FR-19e 三條需求編號移除，FR-19b 改寫為新內容，FR-19f～FR-19i 不受影響；同步更新 SPEC.md 系統架構總覽表、NFR-5、NFR-8、風險表、測試策略、實作計畫 Step 2.4 說明、待確認事項註記，ADR-7 狀態改為 superseded；本次僅為規格層級調整，尚未動任何程式碼，待 Robin 確認 SPEC 內容後才會開始實作 | Claude（依 Robin 提出的簡化方向調整規格，待確認後實作） |
| 2026-08-05 | **Phase 2 Step 2.4 完成：錯誤 log 雲端連結（FR-19b）**。Robin 確認 SPEC.md 調整內容後指示「開始開發吧」。`webhook.py` 新增 `_upload_error_log()`（封裝既有 `GDriveClient.upload_file()`，環境變數缺失或 Drive API 例外皆優雅降級回傳 `None`，不影響訊息送出）與 `_ERROR_LOG_FILE_TEMPLATE`（完整、不截斷的 log 內容）；`_notify_robin_of_error()` 延伸為捕獲例外時額外上傳 log 檔案（檔名 `error_log_{時間戳記}_{觸發功能}.log`），成功則在私訊 Robin 的訊息末尾附加連結；其他使用者收到的安全用語完全不受影響。新增 6 個測試涵蓋上傳成功/失敗/環境變數缺失等情境，既有測試不需修改即全數通過；全專案 709 個測試全過，`webhook.py` 達到 100% 覆蓋率 | Claude（依 Robin「開始開發吧」指示實作） |
| 2026-08-05 | **新增 ADR-16：Telegram 故障時的 email 備援通知**。Robin 驗收 Step 2.4 時提出「如果壞掉的是 Telegram 本身，不就沒辦法通知？」，確認這是原設計沒考慮到的單點故障。新增 `submodules/email`（`EmailClient.send_text()` 用 `smtplib` 直打 Gmail SMTP，複用既有 `GMAIL_USER`／`GMAIL_PASSWORD`，原為 Phase 3 FR-23 預留但尚未使用）；`webhook.py` 的 `_notify_robin_of_error()` 拆成「組裝內容」與「透過 Telegram 送達」兩段，只有 Telegram 送達失敗才觸發新增的 `_send_email_fallback()`，寄出內容含完整 Traceback 的備援信；email 本身也失敗只記 log，不再疊加第三層備援。新增 11 個測試（`submodules/email/client.py` 5 個、`webhook.py` 6 個），全專案 720 個測試全過，`webhook.py`／`submodules/email/client.py` 皆達到 100% 覆蓋率 | Claude（依 Robin 提出的問題新增備援機制） |
| 2026-08-05 | **新增 Step 2.7、FR-66、ADR-17（規格層級，尚未實作）：Google Calendar 整合**。Robin 詢問 Google Calendar 能拿來做什麼，討論後確認範圍：① 待辦事項、② 重要通知（生日/節日）、③ 體態目標期限，三者單向同步寫入 Robin 家庭共用的 Google 行事曆事件，家人可透過手機原生行事曆 App 直接瀏覽，或照舊詢問 Robinson；暫不做「讀行事曆查空檔」（進階功能，工程量高、先觀察實際需求）。新增 robinson SPEC.md FR-66（＋66a～66d 子項）、ADR-17（設計決策：單一共用行事曆、僅 Robin 本人 OAuth 授權、Step 2.7）；同步新增 submodules-core SPEC.md FR-12、Step S.12、ADR-12（`submodules/calendar` 規劃：`CalendarClient` 用 Google Calendar API v3，OAuth 2.0 但使用獨立一組憑證與 `calendar.events` scope，不與 `gdrive` 共用憑證，符合最小權限原則）。本次僅為規格文件調整，程式尚未動工，待 Robin 完成 Google Cloud Console 手動設定後才會開始實作 | Claude（依 Robin 討論結果補上規格） |
| 2026-08-05 | **Phase 2 Step 2.7 完成：Google Calendar 整合（FR-66、ADR-17）**。Robin 確認家人共用一律用免費 Google 帳號（唯讀權限），並提出兩個補充設計：家人共用權限固定「查看所有活動詳細資料」避免覆寫衝突（Google Calendar 共用設定本身解決，非程式碼）、待辦事項/體態目標各自新增一輪「要不要同步」反問保護隱私（ADR-17 決策 6／7）。Robin 完成 Google Cloud Console 手動設定後指示「可以開工了」。新增 `submodules/calendar/client.py`（`CalendarClient`，10 個測試，覆蓋率 100%）；`todos`／`body_goals` 依 ADR-10 核准新增 `sync_to_calendar`／`google_calendar_event_id` 欄位（`0031`／`0032` migration）；三處整合：待辦事項新增流程多一輪 `pending_todo_calendar_sync` 反問（單一時間點待辦預設 30 分鐘事件時長，區間待辦用實際起訖時間），標記完成/取消時同步刪除事件；重要通知（節日/生日）判斷通過就自動建立全天事件，不逐筆詢問；體態目標只有講清楚期限才會多問 `pending_goal_calendar_sync`，達成/取消時刪除事件。所有 Calendar 呼叫皆優雅降級（環境變數未設定或 API 例外都不影響原功能），`webhook.py`／`main.py` 新增 `_build_calendar_client()`；全專案 758 個測試全過，`submodules/calendar/client.py`／`src/bot/body.py`／`src/bot/notifications.py` 達到 100% 覆蓋率 | Claude（依 Robin「可以開工了」指示實作） |
| 2026-08-07 | **Phase 2 Step 2.5 完成：外部 API 重試機制（FR-19i）**。Robin 確認繼續 Step 2.5 後，經 AskUserQuestion 確認三個設計問題：① 程式碼放置方式選「抽成共用 retry 工具」（而非 6 個 client 各自複製，見 submodules-core SPEC.md ADR-13，是 ADR-4「子模組彼此獨立、互不 import」的刻意例外）② 重試判斷標準選「只重試暫時性錯誤」（連線失敗、逾時、HTTP 429／5xx），永久性錯誤（401/403/404 等）直接往外拋不浪費重試次數 ③ 套用範圍確認這次只套用到 6 個現有子模組，104 求職爬蟲 API 留到 Phase 4 開工時比照。新增 `submodules/retry/`（`call_with_retry()`，只負責重試迴圈與 Exponential Backoff 1s/2s/4s 時間控制，`is_retryable` 判斷式由呼叫端傳入）；`llm`／`telegram`／`voice`／`gdrive`／`calendar`／`email` 六個 `client.py` 都套用，各自定義符合自己 SDK 例外型別的判斷邏輯（例如 `llm` 檢查 `ServerError`／`ClientError(429)`，`email` 排除 `SMTPAuthenticationError`）；`LLMClient` 既有的本地端節流保護（`LLMQuotaGuardError`）刻意留在重試包裹範圍之外，因為節流是時間窗口邏輯，立即重試無意義。TDD 全程 RED→GREEN：先寫 `retry` 的 6 個測試，再逐一為 6 個子模組補上重試情境測試；全專案 795 個測試全過，7 個子模組（含新增的 `retry`）皆維持 100% 覆蓋率。本次只完成「重試機制本身」，3 次全部失敗後把最後一次的原始例外原封不動往外拋出，「依錯誤來源進入 FR-19f／FR-19g 分級流程」留待 Step 2.6 在這個基礎上建立 | Claude（依 Robin 於 AskUserQuestion 確認範圍後實作） |
| 2026-08-07 | **Phase 2 Step 2.6 完成：例外分級降級與決策執行狀態閉環回饋（FR-19f～FR-19h），Phase 2 全部 7 個 Step 全數完成**。Robin 指示「接著開發 2-6，做完我再一起 git push」後開工，建立在 Step 2.5 重試機制之上：3 次重試全部失敗才正式判定失敗，才進入分級。`webhook.py` 新增 `_is_llm_failure(exc)` 分類判斷式——用 `LLMQuotaGuardError`（本地端節流保護）與 `google.genai.errors.APIError`（涵蓋 Gemini 官方 `ServerError`／`ClientError`）兩種「唯獨呼叫 LLM 才會拋出」的例外型別，準確區分「LLM API 本身掛掉」（FR-19g 重大疾病級）與「其他元件異常」（FR-19f 一般感冒級），不會跟 DB／GDrive／Calendar／Telegram 的例外混淆；新增 `_GENERAL_COLD_REPLY`／`_MAJOR_ILLNESS_REPLY` 兩句固定範本（不經 LLM 生成）；`_notify_robin_of_error()` 新增 `severity` 參數，重大疾病級會在私訊內容前加上 `_CRITICAL_SEVERITY_BANNER` 最高等級告警橫幅；新增 `_broadcast_major_illness_to_family()`，重大疾病級時額外廣播給所有已綁定家人（排除 Robin——他走告警橫幅；排除觸發當下的使用者——已透過主流程收到同一句話，避免重複發送）。FR-19h（決策執行狀態閉環回饋）稽核後確認屬於架構層級已滿足、不需要逐一修改各功能模組：`finance.py`／`todo.py`／`mood.py`／`router.py` 皆確認沒有任何 `except` 包住 DB 寫入呼叫（`body.py` 唯二的 `except` 只包住選配的 LLM 估算，不影響 DB 寫入例外傳遞），任何資料異動失敗都會一路往外傳到 `webhook.py` 這個單一進入點被新的分級邏輯接住；成功路徑則從 Phase 1/Phase 2 各模組實作起就已內建明確確認文案，此條無需新增程式碼，僅為驗證與記錄。TDD 全程 RED→GREEN，重新命名既有的 `_UNEXPECTED_ERROR_REPLY` 為 `_GENERAL_COLD_REPLY`（既有測試因為都是動態參照模組屬性，改名後同步更新即可、行為不受影響）；全專案 810 個測試全過，`webhook.py` 維持 100% 覆蓋率（含補上先前一直缺漏的 `_build_calendar_client()` 成功路徑測試）。**Phase 2（記帳＋體態管理＋重要通知＋系統韌性與自主診斷治理）全部 7 個 Step 全數完成，實際完成時間 8/7，早於原訂 8/11 目標**，下一步進入 Phase 3（個人技能成長＋好友模式）。本次先本地 commit，push 留待 Robin 之後一起處理 | Claude（依 Robin「接著開發 2-6」指示實作） |
| 2026-08-07 | **Phase 3 Step 3.1 完成：每日重點技術分享（FR-22、FR-23）**。Robin 指示「從 3-1 開始吧」，經 AskUserQuestion 確認三個設計問題：① TLDR 電子報辨識方式選「寄件者網域比對」（`tldrnewsletter.com`，比主旨關鍵字穩定，因為電子報版本多、主旨格式不保證固定）② IThome／TechCrunch 新聞來源選「RSS Feed」（輕量、不用額外套件）③ 去重機制核准於 `users` 表新增 `skill_growth_pushed_on`（DATE）欄位（Robin 依 ADR-10 核准 `0033_add_skill_growth_pushed_on_to_users.sql`），比照 `todos.daily_pushed_on` 慣例，記錄「今天是否已推播過」。`submodules/email` 新增 `fetch_yesterday_emails_from_domain()`（IMAP，複用既有 `GMAIL_USER`／`GMAIL_PASSWORD`，見 submodules-core SPEC.md ADR-11 追記，+23 個測試共 31 個，覆蓋率 100%）；新增 `submodules/newsfeed`（`NewsFeedClient`，`requests`＋標準函式庫 `xml.etree.ElementTree` 解析 RSS，不裝 `feedparser`，見 ADR-14，22 個測試，覆蓋率 100%）；新增 `src/bot/skill_growth.py`：`check_and_push_daily_digest()` 固定台灣時間 08:00 推播給 Robin，三個來源（TLDR 電子報／IThome／TechCrunch）任一抓取失敗只記 log、視為當日無內容，不影響其他來源與整體推播（來源容錯，避免單一新聞來源改版拖累其他來源）；三個來源都沒內容時仍推播固定的「今天沒有新內容」訊息，不靜默跳過（NFR-10）；`skill_growth` 功能開關（`owner_only=True`，已存在於 `templates.FEATURE_LIST`）關閉時跳過，不消耗 Gemini API 額度；去重用「來源日期」（每天只處理「昨天」這一天固定範圍）＋`users.skill_growth_pushed_on` 兩層機制，天然涵蓋 NFR-11 要求，不需要額外的內容雜湊表；19 個測試，覆蓋率 100%。Gemini 呼叫改用新增的獨立 `GEMINI_API_SKILL_GROWTH_KEY`（第 5 把 Gemini Key，比照 `GEMINI_API_PRIVACY_KEY` 既有慣例），**Robin 需自行在 AI Studio 申請這把新 Key 並設定到 Render 部署環境，未設定前這個排程檢查會優雅跳過、不影響其他既有功能**。`main.py` 新增 `_check_skill_growth_digest()` 借用 `/healthz` 既有 10 分鐘 cron 頻率。同步補齊前次 Step 2.5／2.6 遺留在 robinson SPEC.md 未打勾的 Step 2.5／2.6／NFR-9／NFR-10 checkbox 與缺漏的變更記錄列（PROGRESS.md 本身當時已正確記錄，只有 SPEC.md 的 checkbox 與變更記錄漏更新）。全專案 878 個測試全過，`src/bot/skill_growth.py`／`submodules/email/client.py`／`submodules/newsfeed/client.py` 皆達到 100% 覆蓋率；本次先本地 commit，push 留待 Robin 之後一起處理 | Claude（依 Robin「從 3-1 開始吧」指示，經 AskUserQuestion 確認範圍後實作） |
| 2026-08-07 | **Step 3.1 當日修正：拆成「23:00 收集／08:00 推播」兩階段，改用 `skill_growth_digests` 表**。Robin 驗收後指出三點：① 明確給出 TLDR 寄件者信箱 `dan@tldrnewsletter.com` ② 排程改為固定台灣時間 23:00 收集「當天」信件與新聞（IThome／TechCrunch 亦同），隔天固定 08:00 才推播，看板固定顯示「前一晚收集到的內容」，不是原本 08:00 當下即時抓「昨天」③ 三個來源都沒抓到內容時，固定回覆「未獲得最新技術分享」（取代原本較口語的訊息）。經 AskUserQuestion 確認：新增 `skill_growth_digests` 表（`digest_date` UNIQUE 防重複收集、`summary_text` 可 NULL 代表三個來源皆無內容、`pushed_on` 記錄推播去重日期），取代原規劃、尚未 push 過的 `users.skill_growth_pushed_on` 欄位，直接修改 `0033` migration 內容。`submodules/email` 的 `fetch_yesterday_emails_from_domain()` 改為 `fetch_emails_from_domain_on_date(sender_domain, target_date)`（呼叫端指定日期，不再假設「昨天」）；`submodules/newsfeed` 移除不再需要的 `fetch_yesterday_articles()` 便利方法；`src/bot/skill_growth.py` 拆成 `collect_and_store_daily_digest()`（23:00 收集並寫入 DB）與 `check_and_push_daily_digest()`（08:00 讀取前一晚那筆資料推播；找不到收集結果時同樣回覆固定訊息並補寫一筆去重標記，避免同一小時內 `/healthz` 多次觸發重複推播）；`main.py` 對應拆成 `_check_skill_growth_collection()`／`_check_skill_growth_push()` 兩個 `/healthz` 檢查函式，分別檢查各自需要的環境變數（收集階段不需要 `TELEGRAM_BOT_TOKEN`，推播階段不需要 Gmail／Gemini 相關變數）。全專案測試更新為 `src/bot/skill_growth.py` 29 個測試、`submodules/email/client.py` 30 個測試、`submodules/newsfeed/client.py` 20 個測試，皆維持 100% 覆蓋率；本次先本地 commit，push 留待 Robin 之後一起處理 | Claude（依 Robin 驗收回饋，經 AskUserQuestion 確認 DB 設計後修正） |
| 2026-08-07 | **功能開關拆分：`skill_growth` 拆成 `tech_intel`／`certificate`／`language` 三個獨立開關**。Robin 表示 TOEIC（證照準備）跟技術情報（YouTube 技術情報／新聞電子報分享）性質不同，一個是語言/硬實力學習追蹤、一個是資訊訂閱推播，不該共用同一把開關；並補充語言學習（英文口說、其他語言，尚未開發）也該獨立於證照準備之外。經 AskUserQuestion 確認 key 命名（`certificate`／`tech_intel`／`language`，比照既有 8 個開關不加模組前綴的命名風格）與既有資料處理方式（既有 `skill_growth` 開關資料搬移到 `tech_intel`，保留原本開啟狀態）。新增 migration `0034_split_skill_growth_toggle.sql`（Robin 依 ADR-10 核准）：動態尋找並移除 `feature_toggles.feature_key` 現有 CHECK 約束、新增涵蓋 10 個代號的新約束，並把既有 `skill_growth` 資料改名為 `tech_intel`；`templates.FEATURE_LIST` 拆成三筆項目；`src/bot/skill_growth.py` 的 `_FEATURE_KEY` 改為 `"tech_intel"`；YouTube 技術情報模組（FR-57～59，尚未開發）確認共用 `tech_intel`。同步更新 robinson SPEC.md（FR-2／FR-22／FR-23／YouTube 章節）、feature-toggles SPEC.md（FR-3 追記、8→10 個模組）、`src/schema/db_schema.md`。測試更新：`test_skill_growth.py` 的 `feature_key` 斷言、`test_toggles.py`／`test_commands.py`／`test_router.py` 硬編碼的模組數量由 8 改為 10（含 `toggle_by_index` 邊界測試 index 由 9 改為 11）。全專案 888 個測試全過，`skill_growth.py`／`toggles.py`／`templates.py` 皆維持 100% 覆蓋率；本次先本地 commit，push 留待 Robin 之後一起處理 | Claude（依 Robin 提出的拆分需求，經 AskUserQuestion 確認命名與資料搬移方式後實作） |
| 2026-08-07 | **Phase 3 Step 3.2 完成：TOEIC 雙軌題庫 Pipeline（FR-24、FR-25a～FR-25f）**。Robin 指示「開發 3-2 吧」並提出「目前雲端還沒有圖片和音檔，應該還是可以開發吧」，經 AskUserQuestion 確認三個設計問題（見 SPEC.md ADR-18）：① Robin 直接把照片/音檔手動丟進 Google Drive 資料夾（檔名 `toeic_{測驗場次代號}_write_{題號}.png`／`toeic_{測驗場次代號}_listen_{題號}.png`／整包 `toeic_{測驗場次代號}_listen.mp3`），機器人固定每週日 22:00 排程掃描比對，而非透過 Telegram 上傳 ② 整包聽力 MP3 自動依語句停頓切割這次一起做（Robin 知情這是尚未經真實錄音驗證的啟發式邏輯）③ Step 3.2 範圍只到「建題庫」，FR-26～FR-30（每日推播/作答/批改）與 FR-24（目標設定/方向建議）留給 Step 3.3。技術面：`submodules/gdrive` 新增 `list_files()`／`download_file()`，OAuth scope 從 `drive.file` 擴大為 `drive.file + drive.readonly`（Robin 需重新執行 `get_refresh_token.py` 取得新 refresh token）；`submodules/voice` 新增 `transcribe_with_segments()`（Groq Whisper `verbose_json` 逐句時間軸）；新增 `src/bot/toeic.py`（檔名解析/分類、Gemini Vision 題目解析、依停頓切割整包音檔＋`pydub`／`ffmpeg`、Gemini 生成單字題並用 `LOWER(target_word)` 唯一索引去重、`run_weekly_pipeline()` 排程進入點）；新增 `toeic_questions`／`toeic_vocab_questions` 表與 `users.toeic_weekly_question_count`（Robin 指定預設 21 題＝一天 3 題）／`toeic_pipeline_last_run_on`（週排程去重）欄位（`0035`～`0037` migration，Robin 核准）；`main.py` 新增 `_check_toeic_pipeline()` 借用 `/healthz` 頻率；Dockerfile 新增 `ffmpeg`（`pydub` 依賴）；**FR-25f 去重規則修正**：原規劃「檔名日期是否在過去一週內」與 Robin 實際確認的檔名格式（無日期）對不上，改用「檔名是否已存在資料庫」判斷。TDD 全程，33 個新測試全過（含用 `pydub` 產生真實靜音音檔＋實際呼叫 `ffmpeg` 驗證切割邏輯可正確解碼，而非只 mock）；本次先本地 commit，push 留待 Robin 之後一起處理 | Claude（依 Robin「開發 3-2 吧」指示，經 AskUserQuestion 確認範圍後實作） |
| 2026-08-07 | **Step 3.2 當日修正：整包 MP3 切割邏輯改為自動判斷「開頭有無作答說明語音」**。Robin 上傳真實錄音 `Test01_Part1.mp3`（6 題、276.7 秒）供實測，發現原始版本（單純取最大的幾個停頓當切割點）會把開頭約 117.6 秒的作答說明語音整段併入第一題（第一段長度 152.8 秒，是其他題目的 5 倍以上）；Robin 確認「有的音檔可能有這種說明語音、有的可能沒有，都要考慮到」。改寫 `src/bot/toeic.py` 的切割邏輯（`_find_split_points()` 移除，改為 `_find_split_plan()`／`_segment_length_variance()`／`_split_points_for_target_length()`）：候選切割點先依「停頓長度至少達全音檔最大停頓的一半」篩選掉句子/說明語音內部的小停頓雜訊，接著把「完全沒有說明語音」與「音檔前 60% 範圍內每一個候選停頓都當作一次可能的說明語音結尾」逐一試切、比較每種切法的「每段長度變異數」，選變異數最小的一組；`split_audio_by_question_count()` 依判斷結果决定是否捨棄開頭一段。用 `Test01_Part1.mp3` 重新實測驗證，正確排除說明語音，6 段題目長度落在 12.5～36.3 秒（多數 27～29 秒），較先前的 152.8 秒異常值大幅改善；新增 `test_split_audio_by_question_count_excludes_leading_instructions_regression()` 用合成音檔重現同樣情境防止未來退化，`_find_split_points` 的舊測試改寫為對應新函式的測試。全專案 940 個測試全過；本次先本地 commit，push 留待 Robin 之後一起處理 | Claude（依 Robin 提供的真實錄音實測回饋修正） |
| 2026-08-07 | **Step 3.2 同日再追加：`exam_type` 泛用化，不寫死證照種類清單**（見 SPEC.md ADR-18 決策 4）。Robin 驗收完問「以後新增 GCP、AWS 等其他證照考試，現有機制能否直接沿用」，確認原設計把 TOEIC 寫死在檔名前綴、資料表名（`toeic_questions`）、Vision Prompt 內文，無法直接套用；Robin 明確要求「exam_type 不能直接鎖死這三類（TOEIC/GCP/AWS），因為會有多種可能，不然就是一律在 Telegram 設定 exam_type」，決定當場泛用化。檔名格式改為 `{exam_type}_{test_id}_write/listen_{題號}.{ext}`，`_FILENAME_PATTERN` 新增 `exam_type` 擷取群組（開放任意小寫英數字串，不加白名單）；`parse_filename()` 回傳值新增 `exam_type`，`classify_drive_files()` 分類 key 從 `(test_id, qnum)` 改為 `(exam_type, test_id, qnum)`；資料表 `toeic_questions` 重新命名為 **`certificate_questions`** 並新增 `exam_type TEXT NOT NULL` 欄位＋對應索引（Robin 依 ADR-10 核准 `0038_generalize_toeic_questions_to_certificate_questions.sql`，含 `ALTER TABLE ... RENAME` 與 `ALTER INDEX ... RENAME`）；`_insert_question()`／`_is_already_processed()` 同步改用新表名與 `exam_type` 欄位；Vision 解析 Prompt 改為 `{exam_type}` 動態組字（例如「這張『GCP』證照考試的題目照片」）；`sync_track1_from_drive()` 的 Drive 掃描經 AskUserQuestion 確認 Robin 選擇「直接列出整個資料夾所有檔案」（而非改用 Telegram 設定 `exam_type` 這個備案），`gdrive_client.list_files()` 呼叫從原本帶 `name_contains="toeic"` 關鍵字過濾改成不帶任何篩選條件，避免漏掉非 TOEIC 檔案。軌道二（`toeic_vocab_questions`，Gemini 生成單字題）刻意維持 TOEIC 專用不泛用化，因為英文單字題生成玩法跟技術證照考古題性質不同。新增 `test_parse_filename_supports_other_exam_types()`／`test_sync_processes_non_toeic_exam_type_and_scans_whole_folder()` 等測試驗證泛用化正確運作；同步更新 `src/schema/db_schema.md`（`certificate_questions` 變更紀錄）、SPEC.md（FR-25a～FR-25c 追記、ADR-18 新增決策 4）。全專案 942 個測試全過；本次先本地 commit，push 留待 Robin 之後一起處理 | Claude（依 Robin 提出的擴充性需求，經確認明確約束「不能鎖死清單」後實作） |
| 2026-08-07 | **Step 3.3 規格定案（尚未實作），新增 SPEC.md ADR-19**。Robin 詢問 Step 3.3 詳細範圍，經多輪對話與 3 次 AskUserQuestion 確認設計決策：① 每日固定 08:00 推播（比照 `skill_growth_digests` 排程模式）② 正解與詳解在建題庫時就一併存好，不等作答當下才生成 ③ FR-29「圖表呈現」改為 Telegram 純文字彈性自然語言問答（Claude 查證 Phase 2 記帳/體態模組程式碼後確認本來就沒有任何畫圖表模組可移除，圖表統一交給 Phase 4 App FR-64）④ 錯題統計維度沿用現有 `question_type`／`exam_type`，不新增主題標籤 ⑤ 作答紀錄採一張統一表（兩個可為 NULL 外鍵 + CHECK）串連軌道一/軌道二，而非分兩張表 UNION。Robin 進一步提出「正解其實有測驗書可拍」，改為新增 `_ans` 後綴檔名（`{exam_type}_{test_id}_write/listen_{題號}_ans.png`）比對既有題目補正解，取代原本讓 AI 推論正解＋加免責聲明的方案；經 AskUserQuestion 確認缺正解的題目直接排除於每日推播候選池（而非照樣推播待確認、或退回 AI 推論備援）。已改寫 SPEC.md FR-24、FR-25c（追記）、FR-26～FR-30 與新增 ADR-19 記錄完整決策脈絡；PROGRESS.md 同步更新。Robin 指示「都已經確認了，直接開工吧」，開始進入 migration／程式實作階段 | Claude（依 Robin 詢問範圍，經多輪 AskUserQuestion 確認設計後定案規格） |
| 2026-08-07 | **Step 3.3 第一階段實作：答案照片比對機制（FR-27 部分，見 ADR-19 決策 2）**。延伸 Step 3.2 的 `src/bot/toeic.py`：`_FILENAME_PATTERN` 新增選填的 `_ans` 後綴群組，`parse_filename()` 回傳值新增 `is_answer_key`；`classify_drive_files()` 新增 `answer_keys` 分類桶（key 為 `(exam_type, test_id, question_type, question_number)`）；新增 `_ANSWER_VISION_PARSE_PROMPT`／`_parse_answer_vision_output()`／`_parse_answer_image()` 解析答案照片（`CORRECT_ANSWER`／`EXPLANATION` 兩欄位格式，跟題目照片的 Prompt 分開）；新增 `_process_answer_keys()`：比對 `(exam_type, test_id, question_type, question_number)` 找到既有題目後 `UPDATE` 補上 `correct_answer`／`explanation`／`answer_source_filename`，找不到對應題目（例如題目照片還沒上傳）只記警告 log 並略過、不報錯；`sync_track1_from_drive()` 改為在三個既有 `_process_*`（建立題目）之後才呼叫 `_process_answer_keys()`，確保「題目照片與答案照片同一批次一起上傳」也能正確比對。新增 4 個 migration（`0039`～`0042`，Robin 依 ADR-10 核准）：`certificate_questions` 新增 `correct_answer`／`explanation`／`answer_source_filename` 欄位；新增 `answer_logs`（作答紀錄，兩個可為 NULL 外鍵 + CHECK 串連軌道一/軌道二）、`certificate_goals`（FR-24 目標設定）、`exam_official_scores`（FR-30 正式成績）三張新表（後兩張表本次僅建表，尚未接程式邏輯）；同步更新 `src/schema/db_schema.md`。TDD 全程，新增 12 個測試（含既有 `parse_filename`／`classify_drive_files` 測試因回傳值新增欄位而更新斷言），全專案 952 個測試全過，新增程式碼皆有對應測試覆蓋。**Step 3.3 剩餘範圍**（每日 08:00 推播出題、作答與批改對話流程、20:00/23:00 提醒與延期記憶、FR-29 彈性文字問答統計、FR-24 目標設定/方向建議對話、FR-30 正式成績記錄流程）尚未實作——這些各自是獨立的對話式功能模組，比照專案一貫做法留待後續逐一展開，避免一次塞入過多未經確認的 UX 細節；本次先本地 commit，push 留待 Robin 之後一起處理 | Claude（依 Robin「直接開工吧」指示，實作 Step 3.3 第一個明確定案的切片） |

| 2026-08-08 | **Production 事故：`/healthz` 逾時 + Phase 2/3 migration 疑似未套用**。Robin 回報 Render production log 大量 `UndefinedColumn`／`UndefinedTable` 錯誤（`users.monthly_budget`、`body_goals`、`important_notifications_log`、`skill_growth_digests` 皆不存在），研判是 Robin 今天第一次把累積已久的 Phase 2＋Phase 3 本地 commit 一次 push 上去，`src/migrations/runner.py` 依編號套用 migration 時在某個檔案卡住、後續全部未套用（`run_pending_migrations()` 設計為失敗即整批中止，`main.py` 又把錯誤吞掉只寫 log，避免 `/healthz` 掛掉，但也導致錯誤不明顯）；仍需 Robin 提供 Render 開機 log 的「Migration 執行失敗」traceback 才能確認卡在哪個檔案，此部分待補。同時 Robin 提供 cron-job.org 的逾時截圖（08:00:30 那次 `Failed (timeout)`），對照當時 log 發現同一次 `/healthz` 請求耗時超過 40 秒（`00:00:54`～`00:01:05`），超過 cron-job.org 30 秒逾時；根因是 `main.py` 的 `/healthz` 把 10 個排程檢查（Neon 容量、待辦、記帳×3、體態、重要通知、技術摘要×2、TOEIC）全部同步塞在同一次 HTTP request 依序執行，平常大部分因「還沒到時間」秒退，但每天 08:00 剛好有 3 個（待辦每日摘要、重要通知、技術摘要推播）真的要做事，疊加當下又有多個查詢因 migration 未套用而失敗，總耗時輕易破 30 秒。經 Robin 選擇「先修 `/healthz` 太重的問題」：改成 10 個檢查丟進背景 daemon thread（`_run_background_checks()`），`/healthz` 立即回 200 不等待；新增 3 個測試（含用假的 `threading.Thread` 驗證確實透過背景執行緒觸發、且是 daemon），全專案 955 個測試全過。**Migration 未套用的根因仍待 Robin 提供 Render 開機 log 才能確認並修復**，這是接下來優先要處理的事，Step 3.3 剩餘功能開發（每日推播等，本來也會借用同一支 `/healthz`）先暫停，等這兩個 production 問題都解決、且確認未來新功能不會重蹈覆轍再繼續 | Claude（依 Robin 回報的 production 錯誤診斷並修復 `/healthz` 逾時問題） |

| 2026-08-08 | **Production 事故根因找到並修復：migration 卡在 `0018` 的 `IndexError`**。Robin 提供 Render 開機 log，確認卡在 `套用 migration：0018_add_budget_fields_to_users.sql` 這一行，緊接著 `IndexError: tuple index out of range`（`submodules/cloudsql/client.py` L126 `cursor.execute(query, params or ())`）。追查後確認：`0018` 的 `COMMENT ON COLUMN` 註解文字含字面 `%` 字元（「FR-43 50% 門檻預警去重用」），而 `CloudSQLClient.execute()` 舊版邏輯只要 `params` 是 `None` 就退回傳一個空 tuple `()` 給 `cursor.execute()`，觸發 psycopg2 對 query 字串做 `%`-style 格式化解析，字面 `%` 被誤判成參數佔位符、找不到對應值就丟出這個例外——這正是「Phase 2/3 所有 migration 從 0018 起都沒套用到 Neon」的根因；`grep` 確認 `0034_split_skill_growth_toggle.sql` 也含 `%`（`LIKE`／PostgreSQL `format()` 的 `%I`），修好前會是下一個卡點。修正（見 [submodules-core SPEC.md](../submodules-core/SPEC.md) 變更記錄）：`execute()`／`execute_query()` 改為 `params is None` 時完全不帶第二個參數呼叫 `cursor.execute(query)`，不觸發 % 解析；新增/改寫 6 個測試，全專案 958 個測試全過。本次先本地 commit，**尚未 push**——需要 push 一次才會真的觸發 Render 重新部署、讓 migration 從 `0018` 開始重跑 | Claude（依 Robin 提供的 Render 開機 log traceback 找出根因並修復） |
| 2026-08-08 | **Production 事故解決確認**：push 後 Robin 提供最新 Render 開機 log，確認一口氣套用了 25 筆待處理 migration（`0018`～`0042`，涵蓋 Phase 2 記帳/體態/重要通知、Phase 3 技術摘要/TOEIC/Step 3.3 第一階段），schema 已追上最新程式碼；`/healthz` 逾時修復也已隨同一次部署上線。**兩個 production 事故（migration 未套用、`/healthz` 逾時）皆已解決**，Step 3.3 剩餘功能開發（每日推播/作答批改/提醒/成效問答/目標設定/正式成績）可以恢復進行 | Claude（確認 Robin 提供的部署後 log） |

| 2026-08-08 | **Step 3.3 每日推播/作答細部設計定案，新增 SPEC.md ADR-20**。兩個 production 事故解決後，Robin 針對「每日 08:00 推播出題」「作答批改」進一步確認設計：① 非 TOEIC 證照只能調「每日出題數量」，TOEIC 額外可調「聽力/填空/單字」三軌比例（現階段只有 TOEIC 有實際題庫，其他證照類型的分配邏輯留待真的新增時再設計）② 另一個獨立維度「新題:複習題」比例預設 7:3、所有證照類型通用，複習池只放「最新一次作答結果答錯」的題目（跳過不算），答對一次就移除、不做間隔重複演算法，複習題不夠時用新題補滿 ③ 作答只接受 A/B/C/D，格式不符要先請使用者重新輸入才能繼續批改 ④ 23:00 視為跳過採靜默處理，不主動發通知 ⑤ 彈性排程支援「今天改到別天」「直接取消今天的」「某日期區間每日題數改為 N」三種語意，比照 `budget_overrides` 的「全局預設＋特殊區間覆蓋」模式。新增資料表設計（尚未建表）：`certificate_daily_settings`（每日出題設定）、`certificate_daily_schedule_overrides`（彈性排程覆蓋）、`certificate_daily_assignments`（記錄每天實際推播的題目，供 20:00 提醒/23:00 跳過判斷/複習池計算使用）。已改寫 SPEC.md FR-26～FR-28 並新增 ADR-20；準備開始實作 | Claude（依 Robin 多輪回覆確認設計細節） |
| 2026-08-08 | **Step 3.3 每日 08:00 推播出題機制實作完成（FR-26，ADR-20 決策 1/2/5）**。新增 3 張表（`certificate_daily_settings`／`certificate_daily_schedule_overrides`／`certificate_daily_assignments`，`0043`～`0045` migration，Robin 依 ADR-10 核准）並補齊 `src/schema/db_schema.md` 說明。新增 `src/bot/certificate_quiz.py`：`effective_daily_question_count()` 依「日期區間覆蓋 → 全局設定 → 預設值（6 題）」順序解析當天生效題數；`_split_by_ratio()` 整數比例拆分（餘數依序分給前面項目，總和一定等於總數）；TOEIC 依三軌比例（預設 1:2:3）拆出聽力/填空/單字子池，其他證照類型只有單一混合池；每個子池再依「新題:複習題」比例（預設 7:3）拆分，複習池只取「最新一次作答結果答錯」的題目（同一題後來答對就整個離開候選池，不再是新題也不再是複習題），複習題不夠湊比例時用新題補滿，不因此少出題；`assign_daily_questions()` 寫入 `certificate_daily_assignments` 且同一天重複呼叫具冪等性（直接回傳既有清單，不重複消耗題庫）；`check_and_push_daily_quiz()` 固定台灣時間 08:00、逐一巡覽題庫裡目前有的每個 `exam_type` 各自出題推播，靠「這個 exam_type 今天是否已經指派過」（而非額外的 `pushed_on` 欄位）判斷是否該推播，避免同一小時內 `/healthz` 多次觸發重複推播；單一 exam_type 出題失敗（例如題庫資料異常）只記 log 並跳過，不影響其他證照類型。`main.py` 新增 `_check_certificate_daily_quiz_push()` 掛上既有 `/healthz` 背景執行緒清單。TDD 全程，`tests/bot/test_certificate_quiz.py` 32 個測試、`certificate_quiz.py` 達 100% 覆蓋率；`tests/test_main.py` 同步新增 3 個對應測試。全專案 993 個測試全過。**Step 3.3 剩餘範圍**（作答與批改對話流程、20:00/23:00 提醒與延期指令、FR-29 彈性文字問答、FR-24 目標設定/方向建議、FR-30 正式成績記錄）尚未實作，比照專案一貫做法留待逐一展開確認；本次先本地 commit，push 留待 Robin 之後一起處理 | Claude（延續 ADR-20 定案設計實作出題引擎） |

| 2026-08-08 | **Step 3.3 彈性排程新增第四種語意「平攤」，ADR-20 決策 5/6 補充**。Robin 驗收每日推播出題機制後，補充彈性排程還有一種「把今天的平攤到其他天（儘量挑離今天近的日期）」的用法，並明確要求 Robinson 算完分攤方案後要先列出「幾月幾號各要多幾題」給他確認，同意才寫入、不同意則依他的調整意見重算，不能自動生效。經 AskUserQuestion 確認兩個規則：① 分攤演算法採「明天起連續每天 +1 題，攤完為止」（天數隨延期題數變動，藉此讓單日增幅維持最小）② 若算出來的日期剛好已有 Robin 手動設定過的排程覆蓋，直接跳過、往後找下一個沒有覆蓋的日期，不覆寫既有決定。已更新 SPEC.md ADR-20（新增決策 6、擴充決策 5、後果）與 FR-26 條文；本次僅為規格層級調整，「平攤」語意的實際計算與確認對話流程尚未實作，留待「作答與批改對話流程」（彈性排程四種語意統一在該步驟展開） | Claude（依 Robin 補充的需求，經 AskUserQuestion 確認演算法規則後更新規格） |
| 2026-08-08 | **Phase 2 體態管理擴充：新增腰圍設定（FR-46）**。Robin 要求比照身高新增腰圍設定：不用每天記錄、可在記體重時順便問、或使用者直接說要記錄；明確定位為參考指標、非必要，BMI 計算不使用腰圍。經 AskUserQuestion 確認「記體重後順便問腰圍」的頻率：只有使用者從未設定過腰圍才問，設定過之後不會每次記體重都重複問（想更新要自己說「設定腰圍」）。新增 `users.waist_cm`（`0046_add_waist_to_users.sql`，合理範圍 40～200 公分，比身高體重寬鬆）；`src/bot/body.py` 新增 `is_waist_reasonable()`／`set_waist()`／`get_waist()`，設計與身高完全對稱；`src/bot/commands.py` 新增獨立「設定腰圍」／`/set_waist` 單輪流程，並擴充 `handle_weight_value_step()`：只有「新增一筆體重紀錄」（排除 `/my_weight_logs` 觸發的更新流程）且腰圍尚未設定時才順便詢問，回覆直接輸入公分數字即完成記錄、任何無法解析成數字的回覆一律視為跳過（不強迫明確拒絕，降低摩擦）；`src/bot/router.py` 註冊對應觸發詞（`/set_waist`／「設定腰圍」）與 `pending_waist_value`／`pending_waist_offer` 狀態分派。TDD 全程，`body.py` 達 100% 覆蓋率，新增程式碼皆有對應測試；全專案 1009 個測試全過 | Claude（依 Robin 提出的需求，經 AskUserQuestion 確認詢問頻率後實作） |
| 2026-08-08 | **Phase 4 Mobile App 新增功能說明：藍牙體重計整合（新增 FR-64a，規格層級）**。Robin 已購入支援藍牙廣播的體重計，用 nRF Connect for Mobile 實測確認可從掃描結果的 Manufacturer Data 取得體重值，並提供完整解析公式（Python 對照範例：取 hex 資料索引 2、3 組成 16 位元整數，除以 100 得公斤數）。設計：App 端新增「開始測量」按鈕，點擊後啟動 10 秒藍牙掃描，逾時顯示「未取得您的體重值」、不記錄任何資料；體重紀錄維持雙入口——人不在家沒帶體重計時，同樣可以直接在 Telegram 手動輸入（既有 FR-46 流程不變），兩種入口最終寫入同一張 `body_weight_logs`。這是 FR-64「App 端原則上不寫資料，寫入以 Telegram 為主」的唯一例外（因量測動作天生發生在手機端，硬綁 Telegram 反而脫離使用情境），已同步修正 FR-64 條文加註例外說明。Phase 4 尚未開工，本次純屬規格文件更新，不涉及任何程式碼；App 端裝置篩選（避免掃到其他藍牙裝置）、藍牙權限/未開啟等例外處理、寫入 API 路由與身分驗證細節，留待 Phase 4 開工時展開獨立 spec | Claude（依 Robin 提供的藍牙測試結果與解析公式撰寫規格） |
| 2026-08-08 | **Step 3.3 作答與批改流程 + 20:00 提醒 + 彈性排程對話流程實作完成（FR-27、FR-28，ADR-20 決策 3～6 全數落地，FR-26 全部完成）**。Robin 指示「腰圍設定已經開發好了，再來接著開發這個吧，開發完我一併 push」，並重申「平攤」語意必須先給他確認、同意才寫入、有調整意見就依建議重算再確認一輪。新增 `answer_logs.assignment_id`（判斷「這筆答案對應哪一批每日推播指派」，取代原本靠題目 id 比對、在允許跨日晚補答情境下會不夠精準的做法）、`users.certificate_answer_reminder_sent_on`（20:00 提醒去重，比照 `finance_reminder_sent_date` 既有慣例）兩欄位（`0047`／`0048` migration，Robin 依 ADR-10 核准）。新增 `src/bot/certificate_answer.py`：`get_pending_assignments()` 依「同一 `exam_type` 只看最新一批 `assigned_date`」判斷還可以作答的題目（天然實現 23:00 靜默跳過——只要沒被隔天新一批取代，隨時可以回頭補答）、`build_question_view()` 依 `certificate_question_id`／`vocab_question_id` 組出題目呈現內容（軌道一正解是自由文字，新增 `_extract_answer_letter()` 用 regex 從常見格式如「A.」「(A)」中萃取字母，抓不到視為資料異常直接跳過該題）、批改與寫入、`check_and_push_answer_reminders()` 20:00 提醒。新增 `src/bot/certificate_schedule.py`：`apply_move()`／`apply_cancel()`／`apply_range_override()` 三種語意直接套用，`compute_spread_plan()`／`apply_spread_plan()` 支援「先算提案、列出幾月幾號各加幾題、等待確認、依建議重算」的迴圈（`num_days` 參數為 `None` 時複現原定案演算法「明天起連續每天 +1 題」，帶入具體天數則改用平均分攤＋餘數給前面幾天）；四種語意皆會同步刪除今天已建立但未作答的 `certificate_daily_assignments`（因為 `certificate_daily_schedule_overrides` 只在生成新一天批次時才會被讀取，單純寫入覆蓋不會讓已經生成的今天批次消失）。`commands.py` 新增「開始作答」／`/start_quiz`（`start_quiz_answer()`／`handle_quiz_answer_step()`，一次一題、只接受 A/B/C/D 格式不符原地反問、跨多個 exam_type 依序做完一個再做下一個）與「調整出題排程」／`/adjust_quiz_schedule`（`start_quiz_schedule_adjust()` 有多個 exam_type 才多問一輪、`handle_quiz_schedule_intent_step()` 用 LLM 把自由文字分類成 MOVE/CANCEL/RANGE/SPREAD 四種語意、`handle_quiz_schedule_spread_confirm_step()` 處理 SPREAD 的確認/取消/依建議重算三種分支）；`router.py` 註冊兩個觸發詞與四個新 `pending_*` 狀態分派；`main.py` 新增 `_check_certificate_answer_reminder()` 掛上 `/healthz` 背景排程（第 11 個排程檢查）。TDD 全程，新增 `tests/bot/test_certificate_answer.py`（29 個測試，100% 覆蓋）、`tests/bot/test_certificate_schedule.py`（17 個測試，100% 覆蓋）、`tests/bot/test_certificate_answer_commands.py`（27 個測試，涵蓋 commands.py 新函式邊界情境）、`tests/bot/test_router.py`／`tests/test_main.py` 新增對應整合測試；全專案 1090 個測試全過。**Step 3.3 剩餘範圍**（FR-29 彈性文字問答、FR-24 目標設定/方向建議、FR-30 正式成績記錄）尚未實作，留待逐一展開；本次僅本地 commit，push 留待 Robin 之後一併處理 | Claude（依 Robin「腰圍設定已經開發好了，再來接著開發這個吧」指示實作） |
| 2026-08-08 | **Step 3.3 剩餘範圍全數完成（FR-29 成效彈性文字問答、FR-24 目標設定與方向建議、FR-30 正式成績記錄，Phase 3 個人技能成長主線至此全部完成）**。Robin 指示「一次把 Step 3-3 全部都開發完吧」；經 AskUserQuestion 確認 FR-30「我的正式成績」查詢深度：只做查詢列表，不含修改／刪除（正式成績是「考完就定案」的歷史紀錄，不像體重/記帳需要常態修正，先求簡單，之後真的需要再補）。`certificate_goals`／`exam_official_scores` 兩張表沿用 ADR-19 定案當下已建好的 `0041`／`0042` migration，本次不需新增 migration。新增三個純邏輯模組：`src/bot/certificate_exam_scores.py`（`record_score()`／`list_scores()`／`distinct_exam_types()`／`format_scores_summary()`）、`src/bot/certificate_stats.py`（`compute_daily_period_stats()` 平均只除以有作答天數、列出未作答日期並排除在平均外、依 `question_type` 統計最常錯/最常對；`compute_formal_period_scores()`；供 FR-24 方向建議共用的 `known_exam_types()`）、`src/bot/certificate_goals.py`（`get_goal()`／`set_goal()` UPSERT／`list_goals()`／`build_advice_prompt()` 組出餵給 LLM 生成客製化建議的 Prompt，不走固定範本）。`commands.py` 新增六組對話流程／單次查詢指令：「我要記錄正式成績」／`/log_exam_score`、「我的正式成績」／`/my_exam_scores`、「設定證照目標」／`/set_certificate_goal`（目標時間/分數皆可回覆「跳過」）、「我的證照目標」／`/my_certificate_goals`、「給我讀書建議」／`/certificate_advice`（單一候選 exam_type 直接生成、多候選先反問，抓近 30 天成效＋目標組 Prompt 交給 LLM）、「查詢我的成效」／`/my_quiz_stats`（FR-29 核心設計：不是死板的單欄位反問，而是每輪把使用者已經講過的內容全部疊加起來重新丟給 LLM 解析 exam_type／正式-小考／時間區間／是否比較兩區間，缺哪個就對應反問 `NEED_EXAM_TYPE`／`NEED_SCOPE`／`NEED_PERIOD`，讓使用者可以一次講清楚也可以分次補充；LLM 判斷出的 exam_type 若對不到既有資料清單，視為 UNCLEAR 安全網防呆，避免因為 LLM 幻覺查到錯誤的證照類型）。`router.py` 註冊六個觸發詞與七個新 `pending_*` 狀態分派。TDD 全程，三個純邏輯模組皆 100% 覆蓋率；新增 86 個測試（`test_certificate_exam_scores.py`／`test_certificate_stats.py`／`test_certificate_goals.py` 三個純邏輯模組測試、`test_certificate_exam_scores_commands.py`／`test_certificate_goals_commands.py`／`test_certificate_stats_commands.py` 三個對話流程測試、`test_router.py` 新增 6 個整合測試），全專案 1185 個測試全過。**Phase 3 個人技能成長主線（Step 3.1～3.3）至此全數完成**，僅剩 Step 3.4（YouTube 技術情報模組）與 Step 3.5（好友模式）尚未開始；本次僅本地 commit，push 留待 Robin 之後一併處理 | Claude（依 Robin「一次把 Step 3-3 全部都開發完吧」指示實作） |
| 2026-08-08 | **Step 3.4（YouTube 技術情報模組）開工前規格釐清，新增 ADR-21（supersede ADR-9），純規格文件更新、尚未動工**。Robin 詢問 Step 3.4／3.5 難度時發現書面規格跟原始想法有落差：ADR-9 當時定案「Rule-based Weight（關鍵字比對+頻道權重），不用 ML/語意分析」，但 Robin 澄清他要的其實是「LLM 讀候選影片標題和說明欄判斷是否符合主題」，並非 ADR-9 討論並否決的「下載影片＋Gemini 摘要」，兩者成本量級不同（前者只是把 API 已回傳的文字 metadata 餵給 LLM 分類，比照 FR-29 量級）；同時 Robin 也要求額外參考觀看次數／讚數／留言數，並支援多組主題（非單一關鍵字）。邊界情況（2 組主題如何分配、超過 3 組如何取捨）Robin 明確授權「你自己處理」，Claude 決定採「保底 + 輪替」：1 組出 3 支、2 組各保底 1 支+1 支給分數最高者、3 組以上優先選「距離上次被推播最久」的 3 組各推 1 支（避免冷門主題被熱門主題永遠排擠）。已更新 SPEC.md：FR-57 新增 `description` 欄位與 `videos.list` 統計資料查詢、新增 FR-57a（多主題設定）、FR-58b 改為 LLM 判讀取代 Rule-based、新增 FR-58c（多主題分配與輪替公平性）、FR-59b 更新配額估算（多主題情境約 500～600 Units／次，仍遠低於每日 1,000 Units 保守上限與官方 10,000 Units 免費額度，並向 Robin 說明配額是免費用量上限、非計費機制）；ADR-9 狀態改為 superseded by ADR-21。**下一步**：SPEC 已更新完成待 Robin 確認，確認後才會依 AGENTS.md 中大型實作前的確認流程正式開工 Step 3.4（資料表設計需另外依 ADR-10 流程提出核准） | Claude（依 Robin 澄清的原始需求與授權的邊界情況決策，更新規格文件） |
| 2026-08-08 | **Step 3.4 FR-58a 再修正：移除「排除 Shorts」規則，改為完全交給 LLM 判讀品質，實作計畫確認後開工**。Claude 依 ADR-21 草案列出實作計畫（含 `contentDetails.duration` 解析步驟排除 Shorts）供 Robin 確認時，Robin 澄清「其實沒有限制說要短影片，只要是要品質高的影片啦」——FR-58a 原文的「剔除 Shorts」不是他真正在意的規則。更新 SPEC.md FR-58a（只保留候選清單內重複來源去重，不設時長門檻）與 ADR-21（新增決策 5，說明「品質」與「時長」是兩件事，交給 LLM 綜合標題/說明欄/互動數據判斷更貼近實際需求），連帶簡化實作計畫（不需要解析 `contentDetails.duration`）。Robin 確認「直接開工吧」後正式進入實作階段 | Claude（依 Robin 對 FR-58a 的進一步澄清，更新規格後依確認的計畫開工） |
| 2026-08-08 | **Step 3.4 實作完成：YouTube 技術情報模組全數落地（FR-57～FR-59，ADR-21），Phase 3 個人技能成長主線（Step 3.1～3.4）至此全部完成**。依已確認的實作計畫依序完成：① 新增 `youtube_topics`／`youtube_pushed_videos` 兩張表與 `users.youtube_last_run_on` 欄位（`0049`～`0051` migration，依 ADR-10 流程建立）；② 新增 `submodules/youtube`（`YouTubeClient`，API Key 認證、`search_videos()`／`get_video_details()`，重試邏輯沿用 `submodules/retry`，14 個測試 100% 覆蓋）；③ 新增 `src/bot/youtube.py` 核心邏輯：主題管理（`list_topics()`／`add_topic()`／`remove_topic()`／`format_topics_list()`）、FR-58a 候選去重（`_dedupe_by_video_id()`）、FR-58d 30 天歷史去重（`_filter_recently_pushed()`）、FR-58b LLM 評分（`score_candidates_for_topic()`，格式解析失敗優雅降級為依觀看數排序）、FR-58c 多主題「保底＋輪替」分配（`select_weekly_recommendations()`，單一通用演算法同時滿足 1 組/2 組/3 組以上三種情境，見模組 docstring）、FR-59 每週四 08:00 排程進入點（`check_and_push_weekly_youtube()`）；TDD 過程中發現並修正一個真實邊界案例 bug——同一支影片同時是兩個主題各自分數最高的候選時，原本的保底輪會把它重複算成兩個主題的保底名額，修正為保底輪與補滿輪都會跳過已選中的 `video_id`（59 個測試，100% 覆蓋）；④ `commands.py`／`router.py` 新增三個 Owner 專屬指令：「我的YouTube主題」／`/my_youtube_topics`（單次列表）、「新增YouTube主題」／`/add_youtube_topic`（單輪輸入）、「移除YouTube主題」／`/remove_youtube_topic`（列清單→輸入編號直接刪除，屬低風險可重新新增的操作，不需要待辦事項刪除那種二次確認關卡），新增 12 個 commands 測試＋3 個 router 整合測試；⑤ `main.py` 新增 `_check_youtube_weekly_push()` 掛上 `/healthz` 既有背景排程（第 13 個排程檢查），LLM 評分沿用既有的 `GEMINI_API_SKILL_GROWTH_KEY`（與每日技術分享共用同一把獨立 Key，同屬 `tech_intel` 開關，不需要再申請第 6 把 Gemini Key），新增 3 個對應測試。TDD 全程 RED→GREEN，全專案 1262 個測試全過，`src/bot/youtube.py`／`submodules/youtube/client.py` 皆達 100% 覆蓋率。**FR-59b 配額計數器範圍說明**：本次先實作排程與推播主流程，尚未實作主動累計每日配額用量的計數器（目前每週僅執行一次、單次估算遠低於門檻，風險低），若之後主題數大幅增加導致接近門檻需回頭補上。**Phase 3 個人技能成長主線（Step 3.1～3.4）至此全數完成**，僅剩 Step 3.5（好友模式）尚未開始；本次僅本地 commit，push 留待 Robin 之後一併處理 | Claude（依 Robin「直接開工吧」指示，依已確認的實作計畫完成 Step 3.4） |
| 2026-08-08 | **Step 3.5（好友模式）開工前規格定案，新增 ADR-22，純規格文件更新、尚未動工**。Robin 指出 FR-51／FR-52 原始規格過於簡略，要求開發前先聽 Claude 的設計想法。Claude 盤點既有相依基礎（`mood_journals` 等各模組近期紀錄、`feature_toggles` 的 `friend_mode` 開關已存在且 `owner_only=False`）後提出設計建議，並用 AskUserQuestion 確認三個關鍵決策：① FR-51「心情趨勢圖」跟 FR-44／FR-29 既有「圖表統一交給 Phase 4 Mobile App」的架構決策衝突，Robin 選擇「文字/emoji 摘要，圖表留給未來 App」② Step 3.5 這次範圍，Robin 選擇「只做被動模式（使用者主動觸發聊天）」，不含主動關懷推播 ③ 好友聊天要讀哪些模組的近期紀錄當素材，Robin 選擇「所有用到的功能模組」（動態依該使用者的 `feature_toggles` 開啟狀態＋近期是否有資料判斷，不寫死固定清單，因此 Robin 觸發時可能含技術情報/證照進度，其他家人觸發則只含自己有用的模組）。已更新 SPEC.md：FR-51／FR-52 依三個決策重寫、新增 ADR-22 記錄完整決策脈絡（含「近期」時間窗口先訂 7 天、複用各模組既有查詢函式、不需新增資料表等實作方向）、Step 3.5 實作計畫條目同步更新。**下一步**：SPEC 已更新完成，待 Robin 確認後依 AGENTS.md 中大型實作前的確認流程正式開工 Step 3.5 | Claude（依 Robin「先聽聽看你的想法」指示，經 AskUserQuestion 確認三個設計決策後定案規格） |
| 2026-08-08 | **Step 3.5（好友模式）實作完成（FR-51、FR-52，ADR-22），Phase 3 個人技能成長＋好友模式主線至此全數完成**。Robin 確認規格後指示「可以開工了」，依已定案的三個決策實作：新增 `src/bot/friend_chat.py`——`_DATA_PROVIDERS` 登記表（ADR-22 決策 3「不寫死清單」的具體實作）涵蓋心情小記／待辦事項／體態管理／記帳／證照準備五個目前有個人紀錄資料的既有模組，各自的 provider 複用既有模組函式（`mood.list_mood_journals()`、`body` 的體重/運動/飲食查詢、`finance.list_transactions()`、`certificate_stats` 的統計函式）查詢近期資料並格式化摘要；`_mood_provider()` 實現 FR-51，把心情紀錄轉成 emoji 序列＋正向/低落/起伏不定的文字趨勢判斷（不做圖片圖表）；`gather_recent_context()` 逐一檢查 `feature_toggles` 開啟狀態＋該模組近期（預設 7 天）是否有資料，兩者皆滿足才納入；`build_companion_prompt()` 組出餵給 LLM 的陪伴聊天 Prompt。「近期」定義依模組性質微調：心情/體態/記帳看「過去 7 天新增」的紀錄，待辦事項看「未來 7 天內到期」的項目（比較符合待辦事項「將要發生的事」的語感），證照準備複用既有 `certificate_stats.compute_daily_period_stats()`。新增 `mood.entry_date_of()`（原 `mood._entry_date_of()` 改為公開函式，供 `friend_chat.py` 跨模組取用心情紀錄的實際發生日期，同步更新 `commands.py` 內對應的文件註解）。`commands.py` 新增 `start_friend_chat()`（單輪：查使用者 `role` → 組 Prompt → 呼叫 LLM → 回傳，找不到使用者資料時優雅降級為「這位使用者」通用稱謂）；`router.py` 新增「陪我聊聊」／`/friend_chat` 觸發詞，放在 owner／家人共用的觸發詞區塊（`friend_mode` 開關 `owner_only=False`，比照 `_MY_TODOS_TRIGGERS`／`_MOOD_JOURNAL_TRIGGERS` 的位置），全體使用者皆可觸發；`templates.py` 補上好友模式的觸發詞說明與情境範例（原本是空清單）。TDD 全程，新增 `tests/bot/test_friend_chat.py`（30 個測試，涵蓋各 provider 的時間窗口邊界、開關檢查、Prompt 組裝）、`tests/bot/test_friend_chat_commands.py`（4 個測試）、`tests/bot/test_router.py` 新增 2 個整合測試（owner／一般家人皆可觸發），`friend_chat.py` 達 100% 覆蓋率，全專案 1294 個測試全過。**Phase 3（個人技能成長＋好友模式）至此全部 5 個 Step（3.1～3.5）全數完成**，下一步進入 Phase 4（求職模組＋Mobile App）；本次僅本地變更，尚未 commit／push，留待 Robin 之後處理 | Claude（依 Robin「可以開工了」指示，依已確認的規格完成實作） |
| 2026-08-08 | **`language`（語言學習）功能規劃決議擱置，新增 SPEC.md ADR-23，純規格文件更新**。Robin 詢問「除了 Mobile App 之外還有哪些功能要開發」，Claude 盤點時發現 `language` 開關（2026-08-07 隨 `skill_growth` 拆分建立）從未展開任何功能性需求、也不在 Phase 0～4 任何 Step 內。Robin 明確指示「language 這個可以先擱置，但是規格書和開發進度要有紀錄」——新增 SPEC.md ADR-23 記錄決議脈絡、於 Phase 4 實作計畫區塊補上「語言學習（規劃擱置）」小節、PROGRESS.md 待決事項新增對應條目。開關本身維持存在但功能上無作用，何時展開留待 Phase 4 完成後再議 | Claude（依 Robin 指示記錄擱置決議） |
| 2026-08-08 | **Step 4.1（104 職缺爬蟲）開工前規格定案，新增 SPEC.md ADR-24，純規格文件更新、尚未動工**。Claude 盤點 Step 4.1 原始規格時發現 FR-35「Gemini Web Search 補公司背景」與已經拿掉的 Search grounding（chat-core ADR-5）直接衝突，經與 Robin 多輪討論確認四項決策：① FR-35 公司背景改採「Email 協作」機制取代 Web Search——每週爬完後 Robinson 找出資料庫沒有背景資料的新公司、寫入資料庫（背景留空）、組 CSV（104公司ID／公司全名／地區／產業類型／背景）寄 Email 給 Robin 自己並私訊告知，Robin 查完背景後把 CSV 上傳到既有 Google Drive 共用資料夾、在 Telegram 說「已上傳 XXX.csv」，Robinson 偵測到後抓檔解析回填資料庫；當週沒有新公司則整段流程跳過、不寄信不通知（此設計歷經 Robin 兩次修正：第一版是 Robin 上傳後主動說，第二版加上「Robinson 寫入資料庫在前、寄信後要私訊告知」兩個環節）② 求職模組僅 Robin 一人可用，`job_search` 開關屬性未來實作時改為 `owner_only=True` ③ FR-33 允許同時存多組搜尋條件（不是單組覆蓋）④ FR-34a 深度爬取先採兩階段架構（列表 API 抓清單 → 逐筆打詳情頁補齊完整內容/應徵條件/福利），因為 sandbox 連不到 104 網路、Robin 也沒有現成 API 實測資料，先保守假設列表 API 不夠完整，待之後有實測結果再簡化。已更新 SPEC.md：FR-33／FR-34a／FR-34c／FR-35（全面改寫並拆成 FR-35a～FR-35e）重寫、新增 ADR-24、Step 4.1 實作計畫條目同步更新，求職章節標題加註「僅 Robin 可用」。**後果預告**：`submodules/email` 需要新增支援附件的寄信方法（目前 `send_text()` 只有純文字）；需要新增「已上傳 {檔名}」動態觸發詞規則；需要新增 `job_companies`／`job_search_criteria` 兩張表，欄位與 migration SQL 留待正式開工時依 ADR-10 流程提出。**下一步**：SPEC 已更新完成，待 Robin 確認後依 AGENTS.md 中大型實作前的確認流程正式開工 Step 4.1 | Claude（依 Robin 提供的公司背景協作流程與其他三個決策，更新規格文件） |
| 2026-08-09 | **生產環境回饋修正：`skill_growth_digests` 改為一天多筆、一筆一個來源管道，推播訊息改為三行式精簡格式（新增 SPEC.md ADR-25）**。Robin 實測後回報兩個問題：① 三個來源（TLDR 電子報／IThome／TechCrunch）合併寫在單一 `summary_text`，完全無法判斷哪個來源當天沒抓到內容、還是收集本身異常 ② 推播訊息塞了完整條列原文，Robin 只需要三行結論，並提供了具體的目標格式（「1.TLDR 電子報總結分享：……」「2.ithome新聞總結分享：……」「3.TechCrunch新聞總結分享：……」）。Claude 原提案新增 3 個獨立欄位（`tldr_summary`／`ithome_summary`／`techcrunch_summary`），被 Robin 否決——理由是新增來源就要 `ALTER TABLE` 加欄位，擴充性差；改為正規化設計：新增 `source` 欄位＋`UNIQUE (digest_date, source)`，`summary_text` 保留但只存單一來源精簡總結。舊表僅有 1 筆資料，Robin 授權直接 `DROP TABLE` 砍掉重建（`src/migrations/0052_recreate_skill_growth_digests_per_source.sql`），不做資料搬遷。`src/bot/skill_growth.py` 全面重寫：`_get_digest()`→`_get_digests_for_date()`（回傳 list）、`build_summary_text()`/`_build_summary_prompt()`→`summarize_source()`/`_build_source_prompt()`（三個來源各自獨立呼叫 Gemini、當天無內容寫入固定文字「今日無內容」而非 `NULL`）、`collect_and_store_daily_digest()` 改為一次收集寫入三筆、`check_and_push_daily_digest()` 新增 `_format_digest_message()` 組出三行式訊息，`source IS NULL` 去重標記列邏輯沿用「完全查無資料」情境。同步更新 `src/schema/db_schema.md`、SPEC.md（新增 ADR-25、FR-22／FR-23 修正註記）。TDD 全程，`tests/bot/test_skill_growth.py` 全面重寫（32 個測試），`skill_growth.py` 維持 100% 覆蓋率，全專案 1297 個測試全過；本次僅本地 commit（`1ddf9d2`），push 留待 Robin 之後處理 | Claude（依 Robin 提供的實際輸出截圖與期望格式，經來回確認 schema 設計細節後修正） |
| 2026-08-09 | **Step 4.1 正式開工（Robin 說「push 了，繼續做吧」後依 AGENTS.md 確認流程執行），完成 Phase A（DB migration）＋ Phase B（FR-33／FR-36 對話式收集流程）**。Phase A：依 ADR-24 決策提出 4 個 migration 檔案並取得核准——`0053_add_job_search_fields_to_users.sql`（`users` 新增 `job_resume`／`job_expectation`／`years_of_experience`／`expected_salary_min`／`expected_salary_max`／`job_search_last_run_on`）、`0054_create_job_search_criteria_table.sql`（FR-33 支援多組搜尋條件）、`0055_create_job_companies_table.sql`（FR-35 公司背景，`background` 允許 NULL 代表待回填）、`0056_create_job_postings_table.sql`（FR-34 職缺資料，`applicant_count`／`source_updated_at` 因 sandbox 無法連線 104 網路實測先保守開放 NULL；`is_unliked`／`is_closed`／`score`／`rank` 等 Step 4.2 欄位刻意不在本次建立，避免猜錯規格），同步更新 `src/schema/db_schema.md`；commit+push 過程撞到已知的 `.git/index.lock`/`HEAD.lock` FUSE 掛載限制（見 memory 記錄），用 `mv` 搬移舊 lock 檔繞過後成功 commit，但 sandbox 無 `GITHUB_TOKEN` 無法直接 push，由 Robin 本人執行 `git push` 完成部署。Phase B：新增 `src/bot/job_search.py`（`save_search_criteria()`／`save_profile()`／長度與年資合理性檢查），`src/bot/commands.py` 新增 8 輪對話流程（搜尋條件 LLM 解析 → 週限制說明＋準備確認 → 履歷全文＋PII 遮蔽＋確認迴圈 → 期望工作敘述＋確認迴圈 → 結構化年資 → 結構化期望薪資下限 → 上限，最後一步才一次寫入 `job_search_criteria`＋`users` 五欄位，見 FR-56f 情境範例與 ADR-26 決策 1），`router.py` 觸發詞（`/set_job_search`／「我要找工作」／「我最近想要找工作了」）只放在 `is_owner` 分支（`job_search` 開關同步改為 `owner_only=True`，ADR-24 決策 2），`templates.py` FEATURE_LIST 同步更新情境範例含新增的年資/薪資問答。TDD 全程，新增 `tests/bot/test_job_search.py`（12 個）、`tests/bot/test_job_search_commands.py`（22 個）、`tests/bot/test_router.py` 新增 2 個整合測試（owner 可觸發／家人被擋下落入一般聊天），全專案 1333 個測試全過。**下一步**：Phase C（FR-34 爬蟲，兩階段 API＋延遲＋ETL 去重，sandbox 無法連線 104.com.tw 實測，先以 mock HTTP 測試為主，待正式部署後第一次真實排程跑完再視實測結果調整欄位/架構）| Claude（依 Robin「push 了，繼續做吧」指示，依已核准的計畫與 schema 執行實作） |
| 2026-08-09 | **Step 4.1 一次做完 Phase C～F（Robin 說「已 push，請直接把 C～F 一次開發完吧」），FR-34（爬蟲）＋FR-35（公司背景協作）＋週排程掛載全數實作完成，Step 4.1 全數完工**。Phase C：新增 `submodules/job104/client.py`（104 公開 AJAX API Client，`search_list()`／`fetch_job_detail()`，UA/Referer 標頭固定套用，欄位解析邏輯明確標註「尚未經真實流量驗證」，見模組 docstring 與 README「⚠️ 待驗證」段落）；`src/bot/job_search.py` 新增 `crawl_and_upsert_jobs()`（依 FR-33 各組條件逐一爬取，兩階段架構，`_polite_delay()` 在每一次 HTTP 請求後強制 2～4 秒延遲）、`upsert_company()`（FR-35a 找新公司、不覆蓋既有背景）、`upsert_job_posting()`（FR-34d ETL 去重，以 `job_id_104` 判斷 INSERT/UPDATE，`first_seen_at` 只在新增時寫入）。Phase D：`submodules/email/client.py` 新增 `send_text_with_attachment()`（標準函式庫 `email.mime`，中文檔名用 RFC 2231 三元組編碼避免亂碼）；`job_search.py` 新增 CSV 組建/解析（`build_new_companies_csv()`／`parse_companies_csv()`，標準函式庫 `csv` module）與回填（`apply_company_backgrounds()`，找不到的 ID 列出提醒人工處理）；`router.py` 新增 `_UPLOADED_FILE_PATTERN`（「已上傳 XXX」regex 觸發詞，比照 `_CLEAN_TARGET_DIALOG_PATTERN`）＋`commands.handle_company_csv_uploaded()`；`webhook.py` 新增 `_build_gdrive_client_optional()`（文字訊息分支選配 Drive 存取，環境變數不完整時優雅降級，不影響其餘一般聊天）。Phase E：`job_search.check_and_run_weekly_job_search()` 整合入口（固定台灣時間週一 08:00，`job_search` 開關檢查，`job_search_last_run_on` 去重，串接爬蟲→FR-35a 判斷→寄信→私訊），`main.py` 新增 `_check_job_search_weekly_crawl()` 掛上 `/healthz` 背景排程（第 14 個排程檢查）；`templates.py` `job_search.owner_only` 已於 Phase B 提前改為 `True`，本次確認一致。Phase F：SPEC.md FR-34a～FR-34d／FR-35a～FR-35c／FR-35e 打勾（FR-35d 為 Robin 手動操作步驟，不勾選）、Step 4.1 整體打勾並註記 FR-34a 欄位待真實驗證；PROGRESS.md 階段總覽／下一步段落同步更新為 Phase 4 進行中。TDD 全程，新增 `tests/submodules/job104/test_client.py`（21 個）、`tests/submodules/email/test_client.py` 新增 3 個、`tests/bot/test_job_search.py` 擴充至 35 個、`tests/bot/test_job_search_commands.py` 新增 3 個、`tests/bot/test_router.py`／`tests/bot/test_webhook.py`／`tests/test_main.py` 各自新增整合測試，全專案 1391 個測試全過。**已知限制**：sandbox 無法連線 104.com.tw，`submodules/job104/client.py` 的 JSON 欄位解析僅通過 mock HTTP 測試，尚未經正式部署後的真實流量驗證，需等 Robin push 部署、下一個週一 08:00 真實排程跑完後回頭確認並視情況調整（見 SPEC.md FR-34a 註記）。**下一步**：待 Robin push 本次 commit 部署上線，觀察第一次真實週排程結果，確認無誤後才會依 AGENTS.md 流程正式開工 Step 4.2 | Claude（依 Robin「已 push，請直接把 C～F 一次開發完吧」指示，一次性完成 Step 4.1 剩餘全部範圍） |
| 2026-08-09 | **Step 4.1 FR-34a 真實流量驗證完成（Robin 主動提議「我可以先幫你在終端機測試看看」，透過瀏覽器 DevTools Network 面板手動抓包，取代原訂「等下週一真實排程跑完再確認」的計畫），`submodules/job104/client.py` 欄位對應已修正**。Robin 依序測試：① `curl`／Python `requests` 直接打原本猜測的 `/jobs/search/list` 只拿到 SPA 頁面 HTML（不是 JSON）② 在 Network 面板用 `Cmd+F` 搜尋回應內容關鍵字「薪」，找到真正的列表 API `https://www.104.com.tw/jobs/search/api/jobs`，回應結構是 `{"data": [...]}`（`data` 直接是陣列，不是原猜測的 `data.list`）③ 點進職缺詳情頁比照抓到真正的詳情 API `https://www.104.com.tw/api/jobs/{短代碼}`（短代碼取自職缺網址結尾，跟列表 API 的 `jobNo` 數字型 ID 是不同識別碼，不可混用）。驗證結果：`jobName`／`custNo`／`custName`／`jobAddrNoDesc`／`jobDetail.jobDescription`／`condition.workExp`／`welfare.welfare` 原本的欄位名稱猜測全部正確；應徵人數（`applyCnt`）其實列表 API 就有，不需要等詳情頁，`fetch_job_detail()` 已移除這個欄位改由 `search_list()` 直接回傳（`upsert_job_posting()` 同步改為從列表階段取得）；`fetch_job_detail()` 的參數改用短代碼（`job_slug`）而非 `jobNo`，`search_list()` 回傳新增 `job_slug`（從 `link.job` 網址擷取，新增 `_extract_job_slug()`）；詳情頁 `appearDate` 格式為 `"YYYY/MM/DD"`（跟列表的 `"YYYYMMDD"` 不同），新增 `_normalize_appear_date()` 統一轉成 ISO 格式；薪資篩選確認除了 `scmin`／`scmax` 外還需要 `sctp="M"`／`scstrict=1`／`scneg=1` 才會真的套用。同步更新 `src/bot/job_search.py`（`crawl_and_upsert_jobs()` 改傳 `job_slug` 給 `fetch_job_detail()`，`upsert_job_posting()` 的 `applicant_count` 改讀 `job` 而非 `detail`）、`tests/submodules/job104/test_client.py`（全面改寫，21 個測試依真實回應格式重寫，新增 `_extract_job_slug`／`_normalize_appear_date` 測試）、`tests/bot/test_job_search.py`（所有涉及 `_FakeJob104Client` 的測試補上 `job_slug`／`applicant_count`）。⚠️ 地區（`area`）／產業（`indcat`）篩選參數名稱這次測試沒有實際設定這兩個條件，仍未驗證。全專案 1397 個測試全過，`ruff check` 僅剩一個修改前就存在、不在本次變更範圍內的既有 lint 提示（`_normalize_url()` 的 `PIE810`）。**下一步**：待 Robin push 部署後續觀察真實排程，並視情況補齊地區/產業參數驗證，即可正式解除 Step 4.2 開工前置依賴（ADR-26 決策 3） | Claude（依 Robin 主動抓包提供的真實 API 回應，修正欄位對應與相關程式碼／測試） |
| 2026-08-09 | **Step 4.1 地區/產業篩選機制修正（Robin 回饋「產業篩選可以移除，但是我明明有地區篩選」並附上他自己實測過的地區篩選網址），FR-33 前置依賴全數解除**。Robin 貼出 `https://www.104.com.tw/jobs/search/api/jobs?area=6001008000&...` 確認 `area` 這個地區篩選參數名稱本身正確，但值是 104 自己的地區數字代碼（例如 `6001008000`），不是使用者輸入的地區文字（例如「台北市」）；沒有可靠的代碼對照表，與其送出猜測的代碼冒著篩選失效風險，改為 `submodules/job104/client.py` `search_list()` 不再送 `area` 參數，`src/bot/job_search.py` `crawl_and_upsert_jobs()` 改為對 104 回傳結果的 `region`（`jobAddrNoDesc`）文字做子字串比對篩選——分頁停止判斷刻意維持看**未篩選**的原始清單是否為空，避免某頁篩不到符合地區的職缺就誤判提早停止翻頁。產業篩選依 Robin 指示直接移除：`search_list()` 拿掉 `industry` 參數、`job_search.save_search_criteria()` 拿掉 `industry` 參數、`commands.py` 對話流程不再詢問「產業類型」，`job_search_criteria.industry` 欄位保留在資料庫但不做 migration 刪除（避免非必要的破壞性操作，只是往後不再寫入）。TDD 全程，新增 2 個地區子字串篩選測試（含一個驗證分頁不會被篩選結果誤判提早停止的邊界案例），全專案 1399 個測試全過。**Step 4.2 開工前置依賴（ADR-26 決策 3）正式解除** | Claude（依 Robin 提供的實測地區篩選網址與產業篩選移除指示，修正篩選機制並同步更新對話流程／文件） |
| 2026-08-09 | **`job_postings.is_closed` 新增並串接爬蟲（Robin 回饋「職缺已關閉這個應該沒看到？！」），解決 ADR-26 決策 5 原本懸而未決的「is_closed 能否自動判斷」問題**。Robin 指出稍早驗證欄位時漏看了 104 API 資料裡其實已經有職缺開放/關閉狀態的欄位；回頭檢查先前抓到的真實回應，確認列表 API 每筆職缺物件都有 `jobSwitch`（例如 `"on"`），詳情 API 的 `data.switch` 也有同樣的值，樣本裡全部都是 `"on"`（沒有實際看過「已關閉」時顯示的值，保守採「非 `on` 才算關閉」的判斷邏輯）。Robin 確認後指示「該建立的表要建立」，新增 migration `0057_add_is_closed_to_job_postings.sql`（`ALTER TABLE job_postings ADD COLUMN is_closed BOOLEAN NOT NULL DEFAULT FALSE`）；`submodules/job104/client.py` 新增 `_is_job_closed()`，`search_list()` 回傳新增 `is_closed`；`src/bot/job_search.py` `upsert_job_posting()` 寫入這個欄位，且職缺重新爬到時會同步更新（狀態變化不會卡在第一次爬到時的舊值）。同步更新 SPEC.md（FR-34d、FR-38b～FR-38e、ADR-26 決策 5 追加確認：Excel 不會出現「是否關閉」欄位，FR-38d/e 移除相關文字）與 `src/schema/db_schema.md`。TDD 全程，新增 `_is_job_closed()` 單元測試、`search_list()` 開放/關閉兩種情境測試、`upsert_job_posting()` 新增與更新 `is_closed` 的測試，全專案 1404 個測試全過 | Claude（依 Robin 指出的資料欄位與「該建立的表要建立」指示，新增 migration 並串接爬蟲） |
| 2026-08-09 | **Step 4.2（契合度評分與技能缺口分析）開工前規格定案，新增 SPEC.md ADR-26，純規格文件更新、尚未動工**。Robin 要求把所有需確認的問題一次列出，Claude 分兩輪確認：第一輪盤點發現 Roadmap 誤把「職缺內容解析」標成 FR-36（FR-36 實際是履歷記錄），且 FR-56f 情境範例已示範履歷收集跟 FR-33 搜尋條件是同一輪對話完成，提出 FR-36 應歸屬 Step 4.1；同時列出 FR-37／FR-38 評分維度、排名機制、交付方式等問題。Robin 逐一回覆後，Claude 收斂出剩餘 5 個關鍵問題（技能缺口顆粒度、評分是否要等背景填完、兩個 Excel 回填流程是否分開、`is_closed` 由 API 還是人工判斷、應徵成效追蹤的範圍歸屬）再次確認，全部定案：① FR-36（履歷＋新增結構化年資／期望薪資欄位）確定移入 Step 4.1 ② FR-37 契合度評分僅計算「所屬公司背景已回填」的職缺（背景空白的本次跳過、下次排程自然納入），比對維度含年資／期望薪資（結構化欄位）＋應徵人數／更新時間（視 Step 4.1 實測 104 API 是否可行），批次（非逐筆）呼叫 Gemini 節省額度 ③ FR-38 技能缺口以「104 職缺 ID」為單位（非公司），採「全庫排名」＋「本週新職缺排名」雙重前 30 名機制，皆排除 `is_unliked = TRUE`／`is_closed = TRUE` ④ 交付機制比照 FR-35 人力協作模式：每週寄 Excel（三張工作表：所有職缺推薦／最新職缺推薦／技能缺口）給 Robin、Robin 標記喜好/關閉狀態後上傳 Drive、Telegram 說「已上傳」後回填資料庫；`is_closed` 欄位視 104 API 實測結果決定是否出現在 Excel（可自動判斷則不出現）；兩個 Excel 回填流程（公司背景 CSV vs 職缺推薦 Excel）用檔名各自獨立判斷 ⑤ FR-39（應徵成效追蹤語句）維持 Step 4.3 範圍，本次僅記錄需求。已更新 SPEC.md：FR-36～FR-38 全面重寫並拆出 FR-37a～FR-37d、FR-38a～FR-38f 子項、新增 ADR-26、Step 4.1／4.2／4.3 實作計畫條目同步更新。**下一步**：SPEC 已更新完成，待 Robin 確認後、且待 Step 4.1 完整實作並實測 104 API 後，才會依 AGENTS.md 中大型實作前的確認流程正式開工 Step 4.2 | Claude（依 Robin 兩輪問答確認的設計決策，更新規格文件） |
| 2026-08-09 | **Step 4.2 全數實作完成（Robin 說「來吧」後呈現實作計畫、確認「OK~」後依 AGENTS.md 確認流程執行），FR-37（Gemini 批次契合度評分）＋FR-38（雙重排名＋Excel 交付＋Drive 回填協作）全數落地，Phase 4 求職模組主線至此全部完成**。DB migration `0058_add_scoring_fields_to_job_postings.sql`（`job_postings` 新增 `score`／`recommend_reason`／`skill_gap_note`／`is_unliked` 四欄，依 ADR-10 流程先提出 SQL 並取得核准；刻意不建立 `rank` 欄位——FR-38a「全庫排名」與「本週新職缺排名」兩種排名並存，同一職缺兩種排名名次不同、存成單一欄位語意衝突，改在 `build_ranked_jobs()` 產生 Excel 當下依 `score` 動態計算，不持久化）。`src/bot/job_search.py` 新增：① FR-37 評分——`list_scorable_jobs()`（撈出所屬公司背景已回填的職缺，`FakeCloudSQLClient` 無 JOIN 支援，改在 Python 端用公司 ID 集合過濾）、`score_jobs()`（比照 `youtube.py` `score_candidates_for_topic()` 批次評分模式，每批 15 筆呼叫一次 Gemini，用 `===JOB <編號>===` 區塊格式承載 SCORE/REASON/GAP 三個欄位，單一職缺解析失敗優雅跳過不卡住整批）、`apply_scores()`；② FR-38a 排名——`build_ranked_jobs(scope="all"/"new_this_week")` 動態計算前 30 名，排除未評分／`is_unliked`／`is_closed`；③ FR-38b Excel——`build_job_recommendation_excel()` 用新增依賴 `openpyxl`（全專案第一次讀寫 xlsx，見 `requirements.txt`）產生三張工作表，`send_job_recommendation_email()` 沿用既有 `send_text_with_attachment()`；④ FR-38e 回填——`parse_recommendation_excel()`（以「連結」為比對鍵值，兩張推薦表去重）／`apply_job_preferences()`；⑤ 整合入口——`_run_weekly_scoring_and_recommendation()` 併入既有 `check_and_run_weekly_job_search()`（FR-37d 要求「緊接在該週爬蟲與公司背景流程之後」，重用 `job_search_last_run_on` 去重，不新增排程/去重欄位），沒有可評分職缺或排名結果皆為空時整段跳過寄信（比照 FR-35a 慣例，避免寄出空白推薦信）。`src/bot/commands.py` 新增 `handle_job_recommendation_excel_uploaded()`（比照 `handle_company_csv_uploaded()` 樣板）；`router.py` 擴充 `_UPLOADED_FILE_PATTERN` 分流邏輯新增 `104職缺推薦.xlsx` 分支；`main.py` `_check_job_search_weekly_crawl()` 新增 `GEMINI_API_JOB_SEARCH_KEY`（比照 `GEMINI_API_SKILL_GROWTH_KEY` 每個功能領域獨立一把 Key 的既有慣例），未設定時 `llm_client` 傳 `None`、FR-37/FR-38 整段優雅跳過但不影響爬蟲本身。TDD 全程，`tests/bot/test_job_search.py` 新增 25 個測試（涵蓋批次評分解析、分批呼叫、排名排除規則、Excel 三張工作表內容、回填比對）、`tests/bot/test_job_search_commands.py`／`tests/bot/test_router.py`／`tests/test_main.py` 各自新增對應測試，全專案 1429 個測試全過，`ruff check` 無新增 lint 問題。同步修正 `src/schema/db_schema.md` 一處過時註解（`applicant_count`／`source_updated_at` 先前寫「sandbox 無法實測、若抓不到就略過」，實際已於 Step 4.1 確認可正常取得，更新為反映真實狀態）。**Phase 4 求職模組主線（Step 4.1＋4.2）至此全數完成**，剩餘 Step 4.3（應徵成效追蹤）僅記錄需求、Step 4.4／4.5（Mobile App）為 Placeholder；本次僅本地 commit，push 留待 Robin 之後處理 | Claude（依 Robin「來吧」／「OK~」指示，依已確認的實作計畫完成 Step 4.2） |

| 2026-08-09 | **Step 4.3（應徵成效追蹤）開工前規格定案，新增 SPEC.md ADR-27，純規格文件更新、尚未動工**。Robin 詢問「4.3 有需要確認的部分嗎」，Claude 盤點 FR-39／FR-40 發現只有 Robin 預告的三種狀態語句、細部設計完全空白，經 AskUserQuestion 兩輪確認（第一輪：ID 來源／狀態機是否嚴格順序／外部管道職缺是否要評分／應徵狀態資料結構，共 4 題；第二輪針對「外部職缺也要評分」追問資料結構要獨立新表還是借用既有 104 職缺表）全部定案：① 推薦 Excel 兩張推薦表新增「104職缺ID」欄位，Robin 直接抄 ID 打語句 ② 應徵狀態任意可直接設定不強制順序，新增第四種「未錄取／已婉拒」結束狀態 ③ FR-40 外部管道職缺確實要納入 Gemini 評分（不只是記錄） ④ 應徵狀態存成獨立歷程表（非單一狀態欄位），保留完整時間軸 ⑤ 外部管道職缺用獨立新表儲存，不借用 `job_postings`／`job_companies`（語意混亂、既有爬蟲邏輯要多加判斷式排除）。已更新 SPEC.md：FR-39／FR-40 拆出 FR-39a～c、FR-40a～c 子項、新增 ADR-27、Step 4.3 實作計畫條目同步更新。**下一步**：SPEC 已更新完成，待 Robin 確認後依 AGENTS.md 中大型實作前的確認流程正式開工 Step 4.3（含實際 migration 欄位設計、觸發語句 regex 命名空間、外部職缺評分觸發時機三項留待呈現實作計畫時定案的細節） | Claude（依 Robin「4.3 有需要確認的部分嗎」提問，經兩輪 AskUserQuestion 確認設計決策後定案規格） |

| 2026-08-09 | **Step 4.3 資料結構設計修正：外部管道職缺改用 `source` 欄位共用同一張表，不另建獨立表（ADR-27 決策 5／6 修正）**。Robin 對稍早定案的「外部管道職缺用獨立新表」提出異議：「職缺的表應該要放同一張表，這樣設計才有擴充性，用 source 區分（104、LinkedIn、Cake），沒必要多用一張表」。Claude 認同並指出額外好處：統一表之後，外部職缺只要資料形狀跟 104 職缺一致（`background`／`content` 已填），就會自動流經既有的 FR-37／FR-38a 評分與排名邏輯，不需要為 FR-40b 另外開發一套獨立批次流程；沒有 104 官方 ID 的外部職缺由系統配發合成識別碼（例如 `EXT-3`）寫入既有 `job_id_104`／`company_id_104` 欄位，欄位語意放寬但不重新命名。經 AskUserQuestion 追問一個隨之而來的行為後果：外部職缺會不會自然跟 104 職缺一起被排進每週推薦 Excel 前 30 名，Robin 確認「可以，混在一起排」。已更新 SPEC.md ADR-27（決策 5／6 改寫、理由/替代方案/後果同步更新）、FR-40a～c 三項子條文、Step 4.3 實作計畫條目。**下一步**：規格已修正完成，待 Robin 確認後依 AGENTS.md 流程正式開工 Step 4.3 | Claude（依 Robin 提出的資料結構異議修正設計，經 AskUserQuestion 確認後續行為決策） |

| 2026-08-09 | **Step 4.3 全數實作完成（Robin 說「開工」後依已確認的 ADR-27 設計執行），FR-39（應徵成效追蹤）＋FR-40（外部管道職缺）全數落地，Phase 4 求職模組主線（Step 4.1～4.3）至此全部完成**。DB migration `0059_add_source_to_job_postings_and_companies.sql`（`job_postings`／`job_companies` 各自新增 `source TEXT NOT NULL DEFAULT '104'`，欄位註解同步放寬 `job_id_104`／`company_id_104` 語意）、`0060_create_job_applications_table.sql`（應徵狀態歷程表，append-only，`status` 不加 CHECK 約束比照 `todos.status` 既有慣例），依 ADR-10 流程先提出 SQL 並取得核准。`src/bot/job_search.py` 新增：① FR-40a `add_external_job()`——先用暫時佔位值分別 `INSERT` `job_companies`／`job_postings`，再用回傳的內部序號組出合成 ID（`EXT-<序號>`）`UPDATE` 回填，公司與職缺序號各自獨立配發；② FR-39b/c `record_application_status()`——寫入前先確認 `job_id_104` 存在，避免 ID 打錯字建立孤兒紀錄，寫入 `job_applications` 全程 append-only；③ FR-39d（開工時追加確認）`list_latest_application_statuses()`／`format_application_statuses()`——依 `created_at` 取每個職缺最新一筆狀態，供「我的應徵紀錄」查詢指令使用；④ FR-38b Excel 的 `_RECOMMENDATION_SHEET_HEADER` 新增「104職缺ID」欄位供 Robin 抄 ID。實測確認一個原本 ADR-27 擔心的風險其實不存在：`crawl_and_upsert_jobs()`／`upsert_company()` 的比對鍵值直接取自 104 API 真實回應，天生不會是 `EXT-` 開頭，不需要額外加 `source` 過濾條件就不會誤動外部職缺。`src/bot/commands.py` 新增六輪對話流程（`start_add_external_job()` 依序收集管道→職稱→公司名→連結→內容→公司背景，內容/背景套用既有 3500 字上限＋FR-13 個資遮蔽，不像 FR-36 那樣有 LLM 解析/確認迴圈，欄位都是直接記錄）與 `handle_my_applications()` 查詢指令。`router.py` 新增「新增外部職缺」／`/add_external_job`、「我的應徵紀錄」／`/my_applications` 觸發詞，以及 `_APPLICATION_STATUS_PATTERN`（`ID=<ID> 職缺已應徵／已獲得面試／已拿到 Offer／已婉拒／未錄取`）直接解析寫入，不走多輪對話狀態機（比照 `_UPLOADED_FILE_PATTERN` 設計）。TDD 全程，`tests/bot/test_job_search.py` 新增 21 個測試（外部職缺建立、應徵歷程 append-only、查詢排序）、`tests/bot/test_job_search_commands.py` 新增 9 個、`tests/bot/test_router.py` 新增 5 個整合測試，全專案 1450 個測試全過，`ruff check` 無新增 lint 問題。**Phase 4 求職模組主線（Step 4.1、4.2、4.3）至此全數完成**，僅剩 Step 4.4／4.5（Mobile App，Placeholder）尚未開工；本次僅本地 commit，push 留待 Robin 之後處理 | Claude（依 Robin「開工」指示，依已確認的 ADR-27 設計執行實作） |
| 2026-08-09 | **Mobile App（Step 4.4／4.5）規格盤點與定案（僅規格，尚未開工實作），見 SPEC.md ADR-28**。Robin 要求先盤點 Robinson 現有所有功能模組，Claude 依 `templates.py` 分類呈現後，Robin 提出一份詳細 App UI/UX 規格（登入頁互動、權限矩陣、首頁選單/頭像下拉選單結構），並明確要求「APP 也可以設定目標和指標，以及各功能開關，不是只有 Telegram 可以設定而已」。此要求與 ADR-14／FR-64 原本「唯讀 BI Dashboard、App 不寫資料」的定位直接牴觸，Claude 新增 ADR-28（supersede ADR-14 相關部分）記錄範疇擴大，而非靜默覆寫已 accepted 的舊決策。**SPEC.md 變更**：FR-64 重寫為「唯讀分析＋可編輯設定」雙軌定位；FR-65 全面改寫為帳密登入（取代原 Token 機制），新增 FR-65b（忘記密碼＝重設新密碼，非復原原密碼）／FR-65c（保持登入，Refresh Token 30 天）／FR-65d（密碼單向雜湊儲存，不留明碼）三項技術決策；新增 FR-67（App icon＋權限矩陣＋首頁佈局）、FR-68（個人基本資訊，唯讀）、FR-69（目標與指標設定，可編輯，含 FR-69a～FR-69e）、FR-70（功能開關）、FR-71（排程設定，僅 Robin，範圍限縮為 6 項既有 owner_only 排程功能）、FR-72（APP設定）。**缺口盤點（回應 Robin「是否有遺漏」）**：發現 Robin 原描述遺漏 3 處——求職關鍵字/地區設定漏了履歷全文／期望工作／年資／期望薪資（FR-37 契合度評分核心輸入）、證照考試類型設定漏了每日出題數量與比例設定、完全沒提到 YouTube 技術情報主題訂閱管理；另確認身高/腰圍屬量測基準值應歸個人基本資訊、心情小記與待辦事項無目標概念不需設定頁、家人生日維持 Robin 於 Telegram 統一管理不開放 App 自行修改。**技術可行性評估**：整體可行，但範疇比原規劃大，建議 Step 4.4／4.5 開工時拆成更細子 Step（登入機制→唯讀分析→個人資訊/目標設定→功能開關/APP設定→排程設定）逐一推進；`users` 表新增欄位規劃改為 `password_hash`／`refresh_token_hash`／`refresh_token_expires_at`（取代原 `app_access_token`），實際 migration SQL 依 ADR-10 流程留待 Phase 4 開工時提案。本次僅規格文件變更，未寫任何程式碼、未跑測試、未涉及 migration | Claude（依 Robin 詳細 App 規格要求，改寫 SPEC.md 並主動盤點缺口＋評估技術可行性） |

| 2026-08-09 | **Mobile App 規格追加確認＋定名「羅賓森」，見 SPEC.md ADR-28 追加確認段落**。Robin 逐項確認 ADR-28 的技術決策：忘記密碼＝重設新密碼 OK；保持登入 Refresh Token 30 天效期 OK（確認理解到期需重新登入）；排程設定範圍限縮 6 項 Robin 專屬排程 OK；技術可行性建議的分階段開工順序 OK；並將 App 命名為「羅賓森」（同步更新 FR-65 App 名稱敘述）。同時 Claude 撰寫使用者體驗方向規劃時，發現原技術細節補充第 2 點的 Expo Router 路由結構寫成底部 Tab Navigation，與 Robin 實際要求的「左上漢堡選單導覽模組頁面＋右上頭像下拉選單」佈局不符，已修正為 Drawer Navigation＋Header 下拉選單設計（見 SPEC.md 技術細節補充第 2 點） | Claude（依 Robin 確認回饋更新 SPEC.md，並修正路由設計不一致之處） |
| 2026-08-09 | **每日技術分享改回深入摘要、拆成三則獨立訊息，並修正 IThome RSS 解析 bug（新增 SPEC.md ADR-29，supersede ADR-25 訊息格式部分）**。Robin 實測 ADR-25 的三行式精簡版本後回饋「寫得太淺，學不到東西」，並附上實際輸出（IThome／TLDR 皆顯示「今日無內容」）；同時指出 IThome 8/8 明明有發一篇 CVSS 9.8 重大漏洞文章卻沒被抓到。Claude 因 sandbox 連不到 `ithome.com.tw` 請 Robin 直接貼出該 Feed 的實際 RSS 原始內容，比對後找到根因：IThome 的 `<pubDate>` 是 `"2026-08-08  10:08:51"` 這種無時區的格式，不是 RSS 慣用的 RFC 822 標準格式，`submodules/newsfeed/client.py` 原本的 `parsedate_to_datetime` 解析邏輯對這種格式一律拋錯，導致 IThome **每一篇**文章都在解析日期這步被跳過——這才是每天顯示「今日無內容」的真正原因，不是真的沒新聞。Robin 也指出 IThome／TechCrunch 過去只用 RSS 標題／連結生成摘要，等於要求 Gemini 編造沒看過的文章內容（「只看標題我也會啊？」），要求真的抓全文；並確認：全文爬取合理（不是搞笑要求）、三則訊息標題格式「每日技術成長摘要-{來源}」、當天無內容的來源完全不推播、抓全文之間要加 1～2 秒禮貌延遲。**修正內容**：① `submodules/newsfeed/client.py` `_parse_pub_date()` 新增 fallback 解析「無時區 `YYYY-MM-DD HH:MM:SS` 格式」（視為台灣當地時間）② 新增 `fetch_article_content(url)`（`requests`+`beautifulsoup4`，優先抓 `<article>` 底下 `<p>`，找不到才退而求其次抓整個 `<body>`，刻意不寫死站台專屬 selector；`requirements.txt`／`submodules/newsfeed/requirements.txt` 新增 `beautifulsoup4`）③ `src/bot/skill_growth.py` 全面重寫：`_enrich_articles_with_content()` 幫每篇文章補正文全文（文章間 1～2 秒隨機延遲）、`_build_source_prompt()`／`summarize_source()` 改為「每篇文章至少 5 句話列點＋總結」格式、拿掉 100 字上限、`_format_source_message()` 取代舊的 `_format_digest_message()`、`check_and_push_daily_digest()` 改為每個有內容的來源各自推播一則獨立訊息（標題「每日技術成長摘要-{TLDR/IThome/TechCrunch}」），無內容來源完全不推播，三個來源全部無內容才維持既有防呆訊息。已更新 SPEC.md（新增 ADR-29、FR-22／FR-23 修正註記）、`submodules/newsfeed/README.md`。TDD 全程，`tests/submodules/newsfeed/test_client.py` 新增 IThome 格式解析與 `fetch_article_content` 測試（29 個測試，100% 覆蓋率）、`tests/bot/test_skill_growth.py` 全面重寫（39 個測試，100% 覆蓋率），全專案測試全過。**發現重要提醒**：此對話與 Robin 本機同步進行的 Phase 4（Step 4.1～4.3 求職模組、ADR-26～28 Mobile App 規格）工作完全獨立平行進行，途中發現 ADR 編號已被 Step 4.3（ADR-27）與 Mobile App（ADR-28）用掉，本次改用 ADR-29，避免編號衝突；本次僅本地變更，尚未 commit，未觸碰 `src/bot/job_search.py` 等 Phase 4 相關檔案 | Claude（依 Robin 提供的實際 RSS 內容找出 bug 根因，並依確認的格式與延遲需求重新設計） |

| 2026-08-09 | **Mobile App 使用者體驗方向第二輪確認＋建立獨立 [docs/specs/mobile-app/SPEC.md](../mobile-app/SPEC.md)**。Robin 逐項確認上一輪提案（運動/心情卡片邏輯、BMI 目標換算、記帳三圖表、求職分析/考試成績內容、技術分享涵蓋每日摘要＋YouTube 週推播），並追加兩項新決策：① **重要通知邏輯修正（見 robinson SPEC.md FR-53f／ADR-30）**：生日/父親節/母親節從「主角排除」改為「全員皆收到、依身份給不同文案」（已擬妥三組文案草案），掃墓提醒改為固定名單（Robin／爸爸／媽媽／弟弟／弟媳／阿姨），Telegram 與 App 兩端皆套用；因為異動已上線功能，本次僅定案文字與邏輯，實際 `notifications.py` 改寫與既有測試修正留待後續獨立 Step 處理，不與 Placeholder 混在一起。② **客訴回饋頁新增「解法」欄位（見 mobile-app SPEC.md ADR-1）**：`complaints` 表新增 `resolution` 欄位，客訴回饋頁破例支援單欄位可編輯（唯讀分析頁的唯一例外），並釐清客訴回饋（FR-60～FR-63，使用者主動觸發）與系統錯誤自動通報（NFR-8）是兩個不相關的功能。由於累積的頁面級 UX 細節已經超出 robinson SPEC.md 「Placeholder」的合理篇幅，依專案既有慣例（比照 feature-toggles／chat-core 等模組）拆出獨立 `docs/specs/mobile-app/SPEC.md`，收納首頁 7～9 張卡片邏輯、左上選單順序（新增「首頁」為第一項）、全域 UX 規則（圖表預設一週可篩選至一個月、缺值斷線、功能關閉灰底提示、兩種空資料文案區分）、8 個模組唯讀分析頁面的圖表/內容規劃；robinson SPEC.md 的 Mobile App 段落改為指向此檔案，僅保留架構層級決策（登入、權限矩陣、可編輯設定頁欄位範圍）。**編號修正**：新增決策原先誤用 ADR-29，發現該編號已被同日另一個平行進行的 Session（每日技術分享 RSS bug 修正）搶先使用，已全面改為 ADR-30 並修正所有交叉引用，避免兩份決策共用同一個編號。本次僅規格文件變更，未寫任何程式碼、未跑測試、未涉及 migration | Claude（依 Robin 逐項確認回饋更新 SPEC.md，並拆出獨立 mobile-app 子 spec） |

| 2026-08-09 | **客訴回饋頁設計修正＋新增 FR-19j（系統錯誤記錄與解法追蹤，Placeholder）**。Robin 澄清上一輪的「解法」欄位提案放錯地方：使用者主動送出的客訴（`complaints`，FR-60～FR-63）他只是查看，不需要解法欄位；真正需要追蹤處理進度的是系統主動推送給他的錯誤回報（NFR-8／FR-19b 既有機制），因為那些他需要實際處理系統才能正常運作；並要求解法記錄不能只能在 App 操作，Telegram 也要能下指令。**修正內容**：撤回 `complaints.resolution` 提案（`complaints` 表維持原樣）；新增 robinson SPEC.md FR-19j（Placeholder）定義新表 `system_error_reports`（發生時間／分級／觸發功能／錯誤摘要——**摘要需先去除 URL 查詢字串再落地，避免例外訊息裡若帶金鑰的 URL 外洩，比照既有「勿印出帶金鑰 URL 的原始例外」慣例**／Drive log 連結／解法）、`_notify_robin_of_error()`（`webhook.py`）擴充為寫入該表並在私訊 Robin 時附上「錯誤ID=N」、新增 Telegram Owner 專屬單行指令「錯誤ID=N 已處理：{解法內容}」（刻意用「錯誤ID=」跟 FR-39b 的「ID=」區隔，避免 regex 撞在一起）。同步更新 [docs/specs/mobile-app/SPEC.md](../mobile-app/SPEC.md)：客訴回饋頁拆成「使用者客訴」（唯讀）與「系統錯誤回報」（含解法可編輯）兩區塊，並重寫 ADR-1 說明本次修正的來龍去脈。本次僅規格文件變更，未寫任何程式碼、未跑測試、未涉及 migration | Claude（依 Robin 澄清回饋修正 SPEC.md 設計，撤回錯誤的 `complaints.resolution` 提案並改設計到正確的位置） |

| 2026-08-10 | **FR-53f（重要通知邏輯修正）＋FR-19j（系統錯誤記錄與解法追蹤）全數實作完成（Robin 說「FR-53f、FR-19j 請開發吧」，`language` 模組與非 TOEIC 證照確認維持擱置不動），兩者皆已從 Placeholder 轉為完成狀態**。**FR-53f**：改寫 `src/bot/notifications.py`——`FIXED_NOTIFICATIONS` 每筆改為 `allowed_roles`（收件人限制，`None`＝全員）＋`subject_role`／`subject_message`（主角文案差異化）兩個獨立正交欄位取代舊的單一 `exclude_role`；新增 `_broadcast_pairs()` 支援「每個收件人各自不同訊息」的送達（`_broadcast()` 改為委派呼叫）；生日迴圈（獨立於 `FIXED_NOTIFICATIONS` 之外）同步改寫，主角收到祝福文案、其他人收到提醒文案；`tomb_sweeping` 改為固定 `allowed_roles`（Robin／爸爸／媽媽／弟弟／弟媳／阿姨）。`tests/bot/test_notifications.py` 同步修正斷言（原「主角被排除」的測試改為「主角與其他人都收到、文案不同」），全部 34 個測試通過。**FR-19j**：新增 migration `0061_create_system_error_reports_table.sql`（依 ADR-10 流程先提出 SQL 並取得 Robin 核准）；新增 `src/bot/system_errors.py`（`sanitize_error_summary()` 去除 URL 查詢字串避免金鑰外洩＋截斷長度、`record_error_report()`、`update_resolution()`、`list_error_reports()`）；`webhook.py` 的 `_notify_robin_of_error()` 新增 `db` 參數（預設 `None`，寫入失敗或未提供皆優雅降級、不影響私訊本身送出），成功寫入時在私訊訊息附上「🔖 錯誤ID=N」；`router.py` 新增 `_ERROR_RESOLUTION_PATTERN`（Owner 專屬單行指令「錯誤ID=N 已處理：{解法內容}」，比照 FR-39b 設計，找不到 ID 時回覆提示）。TDD 全程，新增 `tests/bot/test_system_errors.py`（10 個）、`tests/bot/test_webhook.py` 新增 3 個（`db` 寫入成功／未提供 `db` 優雅降級／寫入失敗不影響私訊送出）、`tests/bot/test_router.py` 新增 3 個（指令成功記錄／ID 不存在／非 Owner 不觸發），全專案 1483 個測試全過，`ruff check` 無新增 lint 問題。SPEC.md 對應 checkbox 已勾選並補上實作註記。本次僅本地 commit（不 push，交給 Robin 自行 push） | Claude（依 Robin「FR-53f、FR-19j 請開發吧」指示，依已確認的 ADR-30／FR-19j 設計執行實作） |

| 2026-08-10 | **首頁新增「體重紀錄」卡片規劃（純規格文件更新，尚未開工）**。Robin 提議首頁需要一張獨立的體重紀錄卡片：內建「開始量測」按鈕、文案「目前體重為 N 公斤」（沒紀錄過則「尚未紀錄過體重」）。更新 [docs/specs/mobile-app/SPEC.md](../mobile-app/SPEC.md) 首頁卡片表：與飲食/運動紀錄同屬體態管理，但顯示邏輯改為「最新一筆」而非「今日」（體重不會每天量，用「今日」邏輯會經常空白）；卡片內建的「開始量測」按鈕直接觸發 FR-64a 既有的藍牙量測流程，是該流程在 App 內的主要入口，不需要先進體態分析頁；按鈕與卡片其餘可點擊區域（導向體態分析頁）互不干擾。順便統一 robinson SPEC.md FR-64a 原本「開始測量」的按鈕文案為「開始量測」，避免同一功能出現兩種措辭。本次僅規格文件變更，未寫任何程式碼、未跑測試、未涉及 migration | Claude（依 Robin 提議的體重紀錄卡片需求更新 mobile-app SPEC.md，並順便統一 FR-64a 按鈕文案措辭） |

## 待決事項

目前**沒有阻塞 Phase 1 開工的待決事項**。

- [x] 確認外部服務金鑰已全數申請完成（Telegram Bot Token、Neon 連線字串、Google Service Account JSON + Drive Folder ID、Gemini x2 Token、Gmail 帳密、GitHub Personal Access Token、YouTube Data API Key）——已於 `.env` 逐項核對存在；`TELEGRAM_BOT_TOKEN`／`YOUTUBE_API_KEY` 已於本次金鑰外洩事故後重新產生
- [ ] `/function` 路由的實際文字模板（分類方式、每個功能的說明文字、是否附操作提示）待有產品原型後由 Robin 補充，見 SPEC.md 附錄 B（不阻塞 Phase 1，FR-56 可先用最簡單的清單格式實作，之後再美化文案）
- [ ] `language`（語言學習）功能規劃**已擱置，非目前 Roadmap 範圍**（2026-08-08，見 SPEC.md ADR-23）：開關已建立但無對應功能，何時展開、展開成什麼內容留待 Phase 4 完成後再議，不阻塞任何現行 Phase
- [x] Step 0.5a（`src/migrations/` + migration runner）已實作
- [x] Step 0.4：Robin 已完成 cron-job.org 設定，確認 API 正常
- [x] Step 4.1 FR-34a：`submodules/job104/client.py` 的 104 AJAX API 欄位解析已於 2026-08-09 由 Robin 透過瀏覽器 DevTools 手動實測驗證並修正（詳見上方里程碑），主要欄位對應／API 路徑皆已確認正確
- [x] Step 4.1 FR-34a 殘留項已解決（2026-08-09）：Robin 補充實測確認地區篩選 `area` 參數名稱正確，但值是 104 自己的地區數字代碼、沒有可靠對照表，改為爬蟲階段對回傳結果做子字串比對篩選，不再送給 104 API；產業篩選依 Robin 指示直接移除（`job_search_criteria.industry` 欄位保留但不再收集/使用）。**Step 4.2 開工前置依賴（ADR-26 決策 3）已解除**

## 下一步

**Phase 3（個人技能成長＋好友模式）全部 5 個 Step 已於 2026-08-08 全數完成**，詳見上方里程碑紀錄與「階段總覽」。

**Phase 4（求職模組＋Mobile App）進行中：**

1. ~~**Step 4.1：104 職缺爬蟲＋公司背景協作＋履歷收集**（FR-33～FR-36，見 ADR-24、ADR-26）~~ **已於 2026-08-09 全數實作完成並經真實 API 驗證，Step 4.1 完整收尾**：FR-33／FR-36 對話式收集流程（搜尋條件＋履歷＋期望工作＋結構化年資/期望薪資，地區篩選改子字串比對、產業篩選已移除）、FR-34 兩階段爬蟲（`submodules/job104` 公開 AJAX API＋2～4 秒隨機延遲＋ETL 去重，固定台灣時間週一 08:00，`is_closed` 自動判斷）、FR-35 公司背景 Email／CSV／Drive 人力協作機制，見上方里程碑紀錄。Robin 透過瀏覽器 DevTools 手動實測驗證，所有欄位假設已修正確認，無待辦事項
2. ~~**Step 4.2：Gemini 契合度評分＋技能缺口分析**（FR-37、FR-38，見 ADR-26）~~ **已於 2026-08-09 全數實作完成，Step 4.2 完整收尾**：FR-37 批次契合度評分（`list_scorable_jobs()`／`score_jobs()`／`apply_scores()`）、FR-38a～FR-38c 雙重排名＋Excel 交付（`build_ranked_jobs()`／`build_job_recommendation_excel()`／`send_job_recommendation_email()`）、FR-38e Drive 回填協作（`parse_recommendation_excel()`／`apply_job_preferences()`／`commands.handle_job_recommendation_excel_uploaded()`），併入既有週排程整合入口，見上方里程碑紀錄。**Phase 4 求職模組主線（Step 4.1＋4.2）至此全數完成**
3. ~~**Step 4.3：應徵成效追蹤**（FR-39、FR-40，見 ADR-27）~~ **已於 2026-08-09 全數實作完成，Step 4.3 完整收尾**：FR-39 應徵狀態歷程記錄（`job_applications` 表＋`record_application_status()`＋「我的應徵紀錄」查詢指令）、FR-40 外部管道職缺對話式新增（`add_external_job()`＋六輪對話流程，與 104 職缺共用 `job_postings`／`job_companies`，`source` 欄位區分來源，自動沿用既有 FR-37／FR-38a 評分排名邏輯），見上方里程碑紀錄。**Phase 4 求職模組主線（Step 4.1、4.2、4.3）至此全數完成**
4. **Step 4.4／4.5：Mobile App「羅賓森」（全功能個人化入口，React Native + Expo）**：**2026-08-09 規格已定案**——架構層級見 SPEC.md ADR-28、FR-64～FR-72；頁面級 UX 細節（首頁卡片、各模組圖表/內容規劃、全域 UX 規則）已展開為獨立 [docs/specs/mobile-app/SPEC.md](../mobile-app/SPEC.md)。App 範疇由唯讀 BI Dashboard 擴大為「唯讀分析＋可編輯設定管理」，登入機制改帳密＋Token。**附帶決議已完成**：FR-53 重要通知邏輯修正（FR-53f／ADR-30）與 FR-19j（系統錯誤記錄與解法追蹤）皆已於 2026-08-10 獨立於 Mobile App 之外實作完成（見上方里程碑），不再是待辦事項。App 本體（Step 4.4／4.5）實作仍為 Placeholder 尚未開工，建議依技術可行性評估拆成子 Step（登入機制→唯讀分析→個人資訊/目標設定→功能開關/APP設定→排程設定）逐一推進

每天對照「建議每日分配」檢查進度。
